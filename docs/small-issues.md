# small issues

* A blinking line cursor should show up in the edit window to mark the location of where text will be inserted when typing.

* cmd+w should close a tab, not the chrome window.

* closing an edit tab with unsaved changes should trigger a 'Save unsaved changes?' modal just like the quit dialog, except the default button is yes.

* harness tabs should have a tab transcript that records the harness output.

* Monitors should have access to tab transcripts.





