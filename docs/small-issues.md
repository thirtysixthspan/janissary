# small issues

* cmd-w in a tab should close the application tab not the native tab, including browser tabs.

* caret in the editor tab should show up on empty lines.

* clicking on the image-meta div should not draw focus away from the editor content. 












