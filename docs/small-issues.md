# small issues

* closing an edit tab with unsaved changes should trigger a 'Save unsaved changes?' modal just like the quit dialog, except the default button is yes.

* harness tabs should have a tab transcript that records the harness output.





