# small issues

* The filename of the edited file should be the name on the editor tab. The length on the tab name will be restricted to the configured max tab name length.

* in the editor window, after typing, the scroll bar resets to the top when it should not.

* edit command should be able to open a new text file. the file will not be saved to disk until saved by the user.

* pressing and holding a key in the editor should repeat the key.

* the flashing editor cursor is too low, it splits lines instead of being aligned with the text

* harness tabs should have a tab transcript

* monitors should have access to the tab transcript

* update next run time in schedule window after the scheduled event is triggered

* web links in agent windows should open web view tabs rather than native chrome tabs.
