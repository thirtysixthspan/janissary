# technical-debt

## ready

* Preserve the reason a file operation failed in `src/file-navigator/filesystem.ts`: `moveReplacingDestination`, `copyItem`, `moveItem`, `renameItem`, and `deleteItem` each collapse every `renameSync`/`cpSync`/`rmSync` throw into a bare `false` or `undefined`, discarding the errno. A permission denial, a full disk, a cross-device rename, and a vanished source are indistinguishable by the time `batch.ts`, `paste.ts`, and `moves.ts` build their `failedPaths`, so `operationFailureText` in `src/file-navigator/operation-report.ts` can only ever tell the user `Could not move 2 of 3 items: a.txt, b.txt` with no cause and no remedy. Return an error-carrying result from the filesystem layer and thread the reason into the notification line. Severity: **medium**.

* Cover the paste half of the undo/redo replay in `src/file-navigator/moves.ts`, which has no colocated test file and is reached only through `manager.test.ts`: the move half is exercised under both conflict policies and the grouped-conflict shape (`preflights all grouped undo conflicts`, `skips grouped undo conflicts`, `a follow-up overwrite call consumes the pending undo entry`), while every paste case calls `manager.undo(label)` with no policy at all. That leaves `performPasteReplay`'s `skip-conflicts` skip and `overwrite-all` branch, the plural `conflicts` result shape for a multi-pair group, and the partial-failure bookkeeping that re-pushes the unreplayed pairs onto the stack entirely untested — on a path whose copy-undo does a recursive `rmSync`. Add a colocated `moves.test.ts` driving `applyStackPaste` directly for those branches. Severity: **high**.

* Unify the two implementations of the undo/redo replay protocol in `src/file-navigator/moves.ts`: `applyStackMove` and `applyStackPaste` each independently preflight conflicts, choose between the singular `conflict` and plural `conflicts` result shape on group size, run the replay under the policy, then perform the same stack bookkeeping — pop the source stack, re-push whatever remained, push what succeeded, and rebuild. The two differ only in whether they address entries by root-relative path or absolute pair and whether an undo copies or deletes. Keeping both in step is manual, and they have already drifted in exactly the way that predicts: the move path grew conflict-policy tests and the paste path did not. Extract the shared preflight-replay-bookkeeping shape over a small per-kind adapter so there is one protocol to test and change. Severity: **medium**.

* Enforce the feature-layering boundary `ai/guidelines/react-code-organization.md` §3 calls "the load-bearing one", which `web/src/` already violates: `web/src/harness/HarnessTab.tsx` imports `../agent-tabs/AgentTabMeta`, and `web/src/agent-tabs/AgentTabBody.tsx` and `InactiveAgentTabBody.tsx` both import `../transcript/Transcript` and `../command-input/CommandArea`/`CommandInput`. The guideline names the remedy — lift the shared part into a shared layer, coordinate in the app shell, or merge the features — and §3 explicitly asks for `import/no-restricted-paths` zones because "a boundary that only exists in a document erodes"; `eslint.config.mjs` configures no import rules at all today. Resolve the five existing imports and add the lint zones so the next one fails the build. Severity: **medium**.

* Colocate the picker-overlay family into its own `web/src/pickers/` feature directory: `HistoryPicker`, `ThemePicker`, `AppThemePicker`, `ThemeListPicker`, `TaskPicker`, `ProfilePicker`, `QueuePicker`, and `TabNavPicker`, together with `PickerOverlays.tsx`, `picker-overlay-props.ts`, `task-picker-keys.ts`, `profile-picker-keys.ts`, `usePopulatePickers.ts`, and the `use*Picker` hooks, sit as roughly thirty loose files among the 175 in the flat `web/src/` root, so a change to overlay behavior means picking them out of an alphabetical pile rather than opening one directory. They are one product capability with one shared props module and one shared renderer, which is the §1 test for a feature directory; the recently completed `ScheduleDialog` colocation is the same move. Severity: **low**.

* Give the bundled file-backed tab plugins a shared helper for external-open and size lookup instead of five near-copies: `src/plugins/audio/activate.ts`, `video/activate.ts`, `image/activate.ts`, `markdown/activate.ts`, and `page/activate.ts` each define their own `openExternal`, with audio and video repeating the same thirteen-line configured-viewer-then-default-then-give-up chain verbatim apart from the noun in the message, and four of them repeat the same `statSync`/`humanSize`/fall back to `'unknown'` block. Audio and video also duplicate an identical `isPlayable` that differs only in which manifest's `fileExtensions` it indexes. The gap is in the contract rather than the plugins: `TabPluginServerCapabilities` exposes `openExternally`, `note`, and `configuredViewer` as primitives but not the composed operation every one of these plugins actually needs. Add the composed helpers next to the plugin API and delete the copies. Severity: **medium**.

* Decouple `web/src/file-navigator/FileNavigatorTab.tsx` from `src/plugins/image/shared.ts`: the file navigator imports the image plugin's concrete `isImagePath` predicate to decide whether a multi-selection opens every selected row, violating §3 (no cross-feature imports), so changing an image extension or replacing the image plugin requires coordinated changes in a core navigation feature and pulls image-specific behavior into that feature. Put multi-open classification behind a shared host-owned file-kind/opener query, or pass the classification into the navigator from the app shell, then remove the concrete plugin import; one web source file imports the predicate, while its definition and tests occupy two image-plugin files. Severity: **high**.

* Move editor interaction rules out of `web/src/editor/EditorTab.tsx`: the 226-line component owns viewport-page calculation, wrapped-row vertical resolution, find-result selection, the full keyboard decision tree, and textarea/IME flushing before it renders JSX, violating §5 (components render, they do not decide), so each input change must be tested through the large rendered editor suite and keeps growing the component that changed repeatedly in recent editor work. Extract those rules into a focused `useEditorInteractions` hook with pure helpers while keeping `EditorTab`'s public props and imperative handle unchanged; one production file imports `EditorTab`, so the extraction stays internal. Resolve by running the `ai/tasks/hygiene/improve-modularity.md` task against `web/src/editor/EditorTab.tsx`. Severity: **medium**.

## development

## deferred

* Give RPC failures and connection loss an explicit result path in `web/src/ws.ts`: `JanusClient.request` ignores the `error` carried by `rpc-reply`, resolves `undefined as T` when the socket is not open, and leaves every pending promise unsettled if the socket closes, while callers such as `web/src/useQuickOpen.ts`, `web/src/file-navigator/useFileNavigatorSearch.ts`, and `web/src/file-navigator/useFileNavigatorMoveOperations.ts` immediately dereference the declared result. Reject or return a typed result on server errors and disconnection, settle pending calls on close/dispose, and make callers handle that shared failure contract. Severity: **high**. — deferred: complexity 8/10, requires a shared RPC failure contract and coordinated lifecycle/error handling across ten client call sites. decision: a lot of work and perf issue - defer until needed.


## declined

* Protect user edits made after a copy-paste before undo deletes its destination in `src/file-navigator/moves.ts`: `undoCopyPaste` records only absolute source and destination paths and unconditionally removes each destination, so editing or replacing a copied file before pressing undo silently deletes the newer content. Record enough identity or content metadata with each copy history entry to detect divergence and surface a conflict instead of removing a changed destination. Severity: **high**. — deferred: complexity 8/10, requires recursive destination identity tracking plus new undo conflict semantics across server history and client conflict handling.

* Stop the sandbox-confinement tests from passing vacuously off darwin in `src/sandbox/index.test.ts`: seventeen cases open with a bare `if (!sandboxAvailable()) return;`, and `sandboxAvailable()` requires `process.platform === 'darwin'` plus `/usr/bin/sandbox-exec`, so on the `ubuntu-latest` runners every job in `.github/workflows/ci.yml` uses, all of them return before their first assertion and are reported as *passing* rather than skipped. Every assertion about the Seatbelt profile the security model rests on — the `-D` param bindings, the secret-deny paths, the credential scrub, the `TMPDIR` override, the offline variant — therefore only ever runs on a developer's Mac, and CI would stay green if the confined path were deleted outright. Convert them to `describe.skipIf(!sandboxAvailable())` (or `it.skipIf`) so a run that cannot exercise confinement reports skips instead of green passes. Severity: **high**. declined: this application is currently limited to running on mac os x.


