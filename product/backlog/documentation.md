# documentation

## ready

* file-navigator-tab (8/10) — 14 of 31 facts missing, 1 wrong. `documentation/user-documentation/tab-types/file-navigator.md` still says the tree has no multi-select and documents only single-row drag/move, single-item conflict handling, and single-item deletion. Missing are click/Shift-click/Cmd-click selection semantics; bulk drag moves; normalization of duplicate, ancestor, descendant, and already-present paths; bulk conflict choices (**Overwrite all**, **Skip conflicts**, **Cancel**); partial-failure reporting; dropping multiple paths into a command bar or editor; bulk deletion and its failure dialog; and the fact that bulk moves are one undo/redo history step. The wrong fact is the explicit one-row-only limitation. Fix by extending the existing file-navigator page with the selection, bulk operation, drag-target, conflict, deletion, and history rules. The ground truth is `product/specs/file-navigator-tab.md`, `web/src/useFileNavigatorSelection.ts`, `web/src/useFileNavigatorMoveOperations.ts`, `web/src/useFileNavigatorDrag.ts`, `web/src/FileNavigatorOverlays.tsx`, and `src/file-navigator/batch.ts`.

* command-routing (7/10) — 8 of 14 facts missing, none wrong. `documentation/user-documentation/command-bar/shell.md` covers only the basics: that an ambiguous line floats a chooser above the command bar, that `↑`/`↓` and `Return` pick and `Escape` cancels, and that the `shell ` prefix skips recognition. Undocumented anywhere: the chooser's option list is always `shell` plus `acp (agent prompt)`, plus one `db query → <name>` per open database; the db route is offered only when the tab actually has an open database; a confident db guess with exactly one open database runs straight away while zero or several open databases fall through to the chooser; the chooser is modal, so the command input is disabled while it is open; a row can be clicked as well as arrowed to; and the `acp (agent prompt)` option is the one highlighted by default. `documentation/user-documentation/command-bar/database.md` and `documentation/user-documentation/advanced-agents/acp-agent.md` contain no routing or chooser content at all, so the `db ` and `acp ` prefixes are never presented as escape hatches the way `shell ` is. Fix by extending the recognition paragraph in `shell.md` and cross-linking it from `database.md` and `acp-agent.md`. The ground truth is `product/specs/command-routing.md` (whose source references are stale — it cites `src/server/resolve.ts`, `src/server/controller.ts`, `src/server/protocol.ts`, and `src/server/recognizers/`, none of which exist after the flattening), `src/recognizers/`, `src/route-choice.ts`, and `web/src/RouteChooser.tsx`.

* root-path (7/10) — 6 of 10 facts missing, none wrong. `documentation/user-documentation/getting-started/tabs.md` has a six-line `$root` section covering the transcript abbreviation, the `~` fallback, and that the shortcut is display-only, and `documentation/user-documentation/automation/profiles.md` covers `$root` inside profile JSON. Undocumented: that the hidden `.janissary` state directory folds into the root so a workspaced clone reads as `$root/workspace/<name>` with the `.janissary` segment elided (longest matching prefix wins); that `$root` is also expanded on *input*, as a path prefix the user can type in `open`, `edit`, and `files`, alongside `~`; that only the start of a path is expanded, leaving a mid-path `$root` or `~` literal; that the root directory itself renders as `$root/`; the specific places the shortcut is applied (the prompt's working directory, the connections panel, an editor tab's metadata header, and the app's own status messages); and that the window titlebar is the one place it is deliberately not applied, reading `Janissary (<version>): <full absolute path>`. Fix by expanding the existing `$root` section in `tabs.md` rather than adding a page. The ground truth is `product/specs/root-path.md`, `src/paths.ts`, and `web/src/useProjectTitle.ts`.

* harness-recording (6/10) — 6 of 20 facts missing, none wrong. `documentation/user-documentation/advanced-agents/harness.md` documents automatic asciicast recordings and on-demand screen captures but has no session-transcript section. Missing are `harness transcript <name>`, its exact-label and error behavior, the normalized transcript's inclusion of subagent activity, its point-in-time editor view, the `.janissary/harness-transcripts/<label>-<timestamp>.txt` location and lazy creation, fresh-launch clearing versus `--relaunch` preservation, and the monitor behavior that delivers transcript history before the current screen while retaining screen-only interactive state. Fix by adding the session-transcript section to `harness.md`, updating its lifecycle/recordings text, and adding the monitor-feed behavior to `documentation/user-documentation/automation/monitoring.md`; a separate page is unnecessary. The ground truth is `product/specs/harness.md`, `product/specs/harness-recording.md`, `product/specs/monitoring.md`, `src/harness/command-parse.ts`, `src/harness/subcommands.ts`, `src/harness/transcript-file.ts`, and `src/monitor/harness-transcript-feed.ts`.

* sidebars (6/10) — 10 of 21 facts missing, none wrong. `documentation/user-documentation/getting-started/tabs.md` covers docking well — the hidden-until-docked rule, the three dockable kinds and their `left`/`right` forms, the one-per-kind displacement rule, sharing a side, the sidebar strip with its per-entry × and double-click rename, and the client-side visible-entry reset — and `documentation/user-documentation/getting-started/keyboard.md` covers `Shift+Tab` section focus. Undocumented: that a docked tab is never the active tab, so docking the active tab moves focus elsewhere and undocking makes it active again; that tab-cycling commands skip docked tabs entirely; that the strip entry's × is consequently the *only* close affordance for a docked tab, since typing `close` cannot reach it while `close <label>` still works; that sidebar visibility is derived and an empty sidebar cannot be shown; that a docked tab does not keep the app alive, so closing the last non-docked tab still quits; that a docked tab vanishes from the central strip and returns to its reordered in-group position on undock; that sidebar width is clamped to a minimum and roughly half the viewport; and that both width and dock placement reset on relaunch and are never persisted. Note that `product/specs/sidebars.md` is stale where it says only two kinds can dock — the schedules tab docks too, per `help.md` and the app. Fix by extending the sidebar section of `tabs.md`. The ground truth is `product/specs/sidebars.md`, `src/tab/dock.ts`, and `web/src/AppShell.tsx`.

* ssh-tab (5/10) — 5 of 17 facts missing, none wrong. Coverage exists but lives inside `documentation/user-documentation/advanced-agents/harness.md` under "SSH sessions" rather than on a page of its own, and it is good: the `ssh <destination>` syntax, verbatim passthrough to the real binary, the bare-host label, the connections-panel `ssh:<destination>` row, `connection close ssh:<name>`, the usage error, the creator-transcript recording, the close-on-exit lifecycle, `send`/schedule delivery, the absence of `as` and `-w`, and the `shell ssh <host>` inline-card carve-out. `documentation/user-documentation/command-bar/connections.md` adds the global `connection list` rows. Undocumented: that an `ssh://` scheme prefix is stripped from the destination and a trailing `:port` is stripped from the label; that duplicate labels are disambiguated with `-2`, `-3`, …; that `connection close ssh:<id>` matches the tab's unique label before its destination, which is what distinguishes two `ssh devbox` tabs, and returns `No open connection ssh:<id>.` on no match; that the connections panel stays visible over an ssh tab even though ordinary harness tabs suppress it; and that ssh tabs are in-memory only and are not restored by `janus --relaunch`. Fix by adding these to the existing "SSH sessions" section rather than creating a page. The ground truth is `product/specs/ssh-tab.md`, `src/ssh.ts`, and `src/ssh-manager.ts`.

* tab-reorder-drag (3/10) — 1 of 10 facts missing, none wrong. `documentation/user-documentation/getting-started/tabs.md` now accurately documents the central tab-strip drag threshold, live preview, group clamp, outside-strip release, Escape cancellation, click/double-click preservation, and post-drag focus. It omits only that dragging a sidebar tab label reorders docked tabs within that sidebar without docking, undocking, or moving the tab to another strip. Fix by adding that one sentence to the sidebar section of `tabs.md`. The ground truth is `product/specs/tabs.md`, `product/specs/sidebars.md`, and `web/src/useTabReorder.ts`.

* transcript (3/10) — 3 of 16 facts missing, none wrong. Coverage is strong and spread across `documentation/user-documentation/getting-started/tabs.md` (the mouse wheel scrolling one line per tick, the scrollbar with its percentage in the command bar, the collapsed `▸ N tool steps  (ctrl+t to expand)` summary line and click-to-expand, clickable `file:line` links, double-click to re-run a prompt line), `documentation/user-documentation/getting-started/keyboard.md` (the accelerating `Shift`/`Ctrl` arrow scrolling, the single-line `Ctrl+P`/`Ctrl+N`, `PageUp`/`PageDown`, `Escape` to return to the bottom, `Ctrl+T`), and `documentation/user-documentation/command-bar/shell.md` (ANSI color and style rendering). Undocumented: that the transcript and command bar are hidden entirely while an interactive program takes the tab over as a full-tab PTY, and restored to exactly their prior state with no new entries when it exits; that new output resets the scroll to the bottom automatically; and that a double-click landing on text already selected from an earlier selection is suppressed rather than re-running the command. Fix by adding these to the "Reading the transcript" section of `tabs.md`. The ground truth is `product/specs/transcript.md`, `src/buffer.ts`, and `web/src/Transcript.tsx`.

## development

* sandbox — flagged by a 190-line spec whose coverage is scattered across `documentation/user-documentation/advanced-agents/workspacing.md` and `workspaced-agent.md`; not yet evaluated fact-by-fact (over this run's limit)

* markdown-rendering — flagged by a 63-line spec with no page of its own beyond `documentation/user-documentation/tab-types/markdown-preview.md`, which covers the tab rather than the rendering rules; not yet evaluated (over this run's limit)

* keyboard-navigation — flagged by section-focus and chord behavior spread between `product/specs/keyboard-navigation.md` and a single `Shift+Tab` row in the docs; not yet evaluated (over this run's limit)

* agent-command-queue — flagged by git activity on queueing and the never-queue command list; not yet evaluated (over this run's limit)

* monitoring — flagged by sustained `feat(monitor)` activity since the last docs pass; not yet evaluated (over this run's limit)

## deferred

## resolved

* append-only-log — documented in documentation/user-documentation/getting-started/activity-log.md (removed 2026-07-24)
