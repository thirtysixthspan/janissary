# documentation

## ready

## development

* file-navigator-tab (8/10) — 4 of 112 facts wrong and 17 missing, and all four wrong ones sit on the controls a user touches first, three of them in the page's own quick-reference tables. `help.md`'s `→` row says an expanded directory moves to its first child and a file is a no-op, while the app re-roots the tree at an expanded directory and opens a file; `documentation/user-documentation/tab-types/file-navigator.md` already states this correctly, so the two surfaces directly contradict each other on the tree's second most-used key. The page's opening section says that either way the 📁 button's "focus moves to the navigator", but the app leaves focus on the tab whose button was clicked, which is exactly why `files in <label>` and the button are not interchangeable even though the page calls the button an alternative to it. The header's location button is described as cycling left sidebar, then center strip, then right sidebar, and as working the same docked or in the strip; the app toggles left and right only, renders no button at all on a center-strip tree, and reaches center by typing the bare command. The mouse table also lists a header × button that the app never renders, contradicting the same page's correct statement that the header has no close button of its own. The 17 missing facts include the `No tab named "<label>".` failure for `files in <label>`, symlinks rendering as leaf files that cannot be expanded, the header's branch readout and what it shows on a detached HEAD, the Split control, the searchable set excluding gitignored files, that a path separator typed into the rename field silently creates a file with that name in its name, that the row context menu is keyboard navigable, that a navigator activation records nothing and never interrupts a busy agent, and that the pull button's failure tooltip points at the notifications tab. Ground truth is `product/specs/file-navigator-tab.md`, `documentation/user-documentation/tab-types/file-navigator.md`, `help.md`, `web/src/file-navigator/file-navigator-keys.ts`, `src/file-navigator/open.ts`, `web/src/shared/dock-cycle.ts`, and `web/src/file-navigator/FileNavigatorHeader.tsx`. Fix by correcting the four claims and adding the missing ones to the existing sections; at 333 lines the page is also at the point where moving files by drag-and-drop and copy/cut/paste want extracting into their own sections. The spec repeats the same three-placement error and its row-context-menu section is truncated.

* notifications (7/10) — 2 of 50 facts wrong and 10 missing, both wrong ones non-core but one of them a `help.md` row a user reads in-app. `help.md` says `notify <message>` is "dropped if the tab is closed", but the app appends to the queue, writes the record, and toasts regardless of whether the feed exists, and `documentation/user-documentation/tab-types/notifications.md` already says the opposite and is right. `documentation/user-documentation/advanced-agents/agent-questions.md` says clicking a `Question from <tab>` line opens the feed in the right sidebar if it was not already up, but the click only focuses the asking tab. The 10 missing facts are mostly reference and rendering detail: the record file's actual field names, which nothing documents although the page calls it a trail to grep; that it is never read back, never rotated and only ever cleared by `notifications clear`; that a write failure is silent and abandoned for the rest of the run; that a toast carries no timestamp and no clickable link targets, which is why a toast cannot be clicked; that a long toast is clamped to two lines; that a docked feed suppresses toasts only on the client showing it; that a replayed notification never toasts; that a docked feed's overflow scrolls inside the feed; and that running `notifications` or `notify` writes a command-echo entry into the issuing tab's transcript. One fact is not a nuance at all: the `Tab plugin "<id>" disabled: <reason>.` line is documented nowhere, three pages link to `command-bar/plugins` for it, and that page has no failure content. Ground truth is `product/specs/notifications.md`, `documentation/user-documentation/tab-types/notifications.md`, `documentation/user-documentation/command-bar/plugins.md`, `documentation/user-documentation/advanced-agents/agent-questions.md`, `help.md`, and `src/notifications/`. Fix by correcting the two claims, adding the missing facts to the existing toast and record sections, and giving the plugin failure line a home on the plugins page.

* tab-plugins (7/10) — 1 of 16 facts wrong and 1 missing, and the wrong one is the page's core content. `documentation/user-documentation/command-bar/plugins.md` says "These six are part of the app" and its sample output shows six rows, but the app ships nine: `pdf`, `conversations`, and `sessions` are absent from both, so a user who runs `plugins` sees nine lines against a page promising six. The same root cause leaves the conversation list and the sessions list out of the page's list of plugin tabs, and neither `documentation/user-documentation/tab-types/conversations.md` nor `documentation/user-documentation/tab-types/sessions.md` says its tab is plugin-backed, so nowhere in the two surfaces is it stated. Everything else the page claims checks out, including the refusal, the column order, the three states, the failure line, failure isolation, and the docking rule. Ground truth is `product/specs/tab-plugins.md`, `documentation/user-documentation/command-bar/plugins.md`, `src/plugins/catalog.ts`, and the per-plugin manifests under `src/plugins/`. Fix by editing the existing page: three more rows in the sample output, drop "six", and name the conversation and sessions lists as plugin tabs. A guard test pinning the documented roster to `tabPluginCatalog` would stop this drifting a third time.

* editor-tab (6/10) — 1 of 50 facts wrong and 4 missing, and it resolves a thread two earlier audits left open. `product/specs/editor-tab.md` has a section pointing at a "CLI → Scrubbing" heading that does not exist in `product/specs/cli.md` and never has, and no masking code exists in the app; the claim is also self-refuting, since an editor tab has no transcript for a keypress to be masked from, so there is nothing to document and the stale spec section should simply be deleted. The wrong fact is the screenshot caption in `documentation/user-documentation/tab-types/editor.md`, which says the tab shows "the unsaved-changes dot next to the filename" when the app renders no such dot for editor tabs — the strip's dirty bullet is fed only from plugin tabs — and an editor tab signals unsaved work solely by enabling the save button. The 4 missing facts are that `edit` refuses a file over 2 MB with a message naming the size and the limit, that re-opening a file already in an editor tab focuses that tab and re-targets its cursor for a `:<line>` argument, that clicking **Commit to origin** while a commit is already running does nothing, and that the unsaved-work close dialog stays bound to the tab it was raised for. Ground truth is `product/specs/editor-tab.md`, `documentation/user-documentation/tab-types/editor.md`, `documentation/user-documentation/tab-types/opening-files.md`, `src/openers/editor.ts`, `src/editor/commit.ts`, `web/src/CloseSaveGuard.tsx`, and `web/src/TabItem.tsx`. Fix by editing the caption and the four gaps in the existing pages; the two spec problems are spec fixes, not doc fixes.

* sandbox (6/10) — 1 of 48 facts wrong and 12 missing, and the wrong one is a security claim a user would act on. `documentation/user-documentation/advanced-agents/workspacing.md` says credentials and secrets, including "the files your AI harnesses keep their own provider keys in", are "invisible, not just unreadable. An agent can't read them and can't overwrite them." For codex that is not true: `~/.codex` is a home write carve-out and the secret deny list has no codex entry, so `~/.codex/auth.json` is readable and writable from inside a workspaced tab, which the repo's own completed plan records as a deliberate interim state. Claude's and opencode's keys are genuinely covered, so the blanket phrasing names all three harnesses and protects two. The 12 missing facts are the shape of the boundary: your shell startup files stay readable so your `PATH` and aliases apply but are never writable, `~/.nvm` and `~/.rvm` are readable so version-manager toolchains work, `~/.cache/opencode/models.json` is readable, setuid binaries such as `ps` cannot run at all, the pasteboard is denied so `pbpaste` returns nothing, Apple Events are denied so `osascript` cannot drive other apps, `TMPDIR` points at a private per-workspace directory, and `/tmp` stays readable and writable but is denied for execution. Ground truth is `product/specs/sandbox.md`, `documentation/user-documentation/advanced-agents/workspacing.md`, `src/sandbox/paths.ts`, `src/sandbox/profile.ts`, and `src/harness/index.ts`. Fix the credential bullet and add a short "what else is reachable" list to the existing page; `JANISSARY_NODE` belongs beside the `$janussary` section of `command-bar/tasks.md`, and the conversation agent's private workspace beside the conversations page. The spec is stale on file paths and omits four carve-outs the app has.

* open (4/10) — 11 of 54 facts missing and none wrong, and every missing one is an error or confirmation string rather than behavior. The behaviors are all documented; the exact wording a user would search for is not, which cuts against the repo's own rule to name the message verbatim. Absent from both surfaces: the not-found and no-matching-files lines, the truncation notice when a pattern matches more than the ten-file cap, both halves of the invalid-address refusal, the refusal to open a web address without the `page` keyword, the per-kind external-open confirmations for a configured application and for each default viewer, the nothing-launchable line that ends with the file's path, the usage errors for bare `video`, `audio`, and `pdf`, the refusal those three give for a file that is not their kind such as `video notes.txt`, and the remote-file message for `open external` that names the notifications feed. Separately, `command-bar/commands.md` has no row for `open` or `edit` at all, though it is the app's main command reference and carries rows for `newfile` and `newdir`. Ground truth is `product/specs/open.md`, `documentation/user-documentation/tab-types/opening-files.md`, `help.md`, `documentation/user-documentation/command-bar/commands.md`, `src/open/file-command.ts`, `src/open/file-manager.ts`, `src/openers/external-viewer.ts`, and `src/plugins/files.ts`. Fix by filling in the existing Errors section, the `open external` section, the three plugin-command rows in `help.md`, and adding the missing `open` and `edit` rows. The spec is wrong about shift-activating a navigator row, which the docs already have right.

* workspaced-agent (4/10) — 5 of 63 facts missing and none wrong, so the 219-line spec genuinely fits its 67-line page. The one worth fixing is a quoted-message gap in a set where every sibling message is quoted: a `-w` launch name must be a single folder name, not empty, not `.` or `..`, and without a slash, and the refusal is never shown, while all three other launch refusals appear verbatim on the agents, workspaced-agent, and remote-agents pages. It is reachable, because the agents page explicitly invites arbitrary names. The other four are that the initial clone is made outside the sandbox and so can still use your SSH transport even though SSH is unusable from inside the workspace afterwards, that a harness workspace folder is named after the tab's unique label so a second `harness claude` gets `claude-2`, that removal is deferred to a background step so deleting a large clone does not freeze the UI, and that the `~/.claude.json` trust entry is removed again when the workspace goes away. Ground truth is `product/specs/workspaced-agent.md`, `documentation/user-documentation/advanced-agents/workspaced-agent.md`, `documentation/user-documentation/getting-started/agents.md`, `src/workspace/label.ts`, and `src/workspace/index.ts`. Fix by adding the refusal beside the other three and folding the rest into the existing sections.

* image-tab (3/10) — 2 of 35 facts missing and none wrong, so the 71-line page covers the tab's commands, every control, both dialogs, the save and PNG semantics, the unsaved rules, and the tab-strip behavior. Missing: that Quick Open on an image opens the tab already in the image editor, because the picker sends `edit` and `edit` on an image flips to editor mode, which `documentation/user-documentation/tab-types/image-viewer.md` does not say and `documentation/user-documentation/command-bar/quick-open.md` words in a way that reads as the plain-text editor; and that the tab-strip × closes an image tab without selecting it first, which the video and web-page pages both state for their tabs. Ground truth is `product/specs/image-tab.md`, `documentation/user-documentation/tab-types/image-viewer.md`, `documentation/user-documentation/command-bar/quick-open.md`, `web/src/pickers/useQuickOpen.ts`, and `web/src/TabItem.tsx`. Fix by adding one clause to each of the two pages.

* tab-completion (3/10) — 1 of 26 facts missing and none wrong, so the 22-line page earns its length. The missing fact is that the multi-match strip is informational: there is no cycling key and no way to pick a candidate, since `Tab` re-queries on each press and the candidates carry no selection state, which invites the natural wrong guess from a page that only says the candidates are listed above the command bar. The ten-row table checks out against the source context by context, and the `monitor` and `unmonitor` material is carried by `help.md` and `documentation/user-documentation/automation/monitoring.md`. Ground truth is `product/specs/tab-completion.md`, `documentation/user-documentation/command-bar/tab-completion.md`, `help.md`, and `src/completion/`. Fix by appending one sentence to the existing page. The real work is the other direction: the spec is badly stale, covering five of the app's ten completion contexts and claiming `msg` and `broadcast` offer active agent names when they offer every open tab's label, so it needs a rewrite to match what the docs already describe.

* markdown-rendering (2/10) — 0 of 17 facts missing and none wrong, so the 30-line page covers the 63-line spec completely. The single-newline line-break rule, the HTML stripping, the plain-text fallback on a parse failure, the claim that each Markdown reply is kept together as one entry, the restriction of Markdown rendering to ACP replies and `help`, the `file:line` link rule including dropping the column, and the centered scroll on a line jump all check out against the source. The busy blink and the plain-text half of the file-link rule live on the tabs and keyboard pages, which are within the two counted surfaces. Ground truth is `product/specs/markdown-rendering.md` and `documentation/user-documentation/advanced-agents/markdown-rendering.md`. No documentation work follows; the spec's file paths are stale and want a refresh by whoever next touches it.

* sessions-tab — flagged by the survey as the next-thinnest spec-to-page mapping after the ten evaluated this run, and by this run's harness finding that a remote session's recording is per attachment; not yet evaluated (over this run's limit)

## deferred

## declined

## resolved

