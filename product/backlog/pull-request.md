<!-- This file is for maintaining work items tied to a pull request and lives on a pull request's own branch while that pull request is open. It should be empty on master, holding no more than this comment and the heading. -->

# pull-request

* auto-approval notifications from remote harness don't seem to trigger a toast. assure all notifications follow the logic for notifications determining where and when the notification is shown in a toast or the notifications tab. 