<!-- This file is for maintaining work items tied to a pull request and lives on a pull request's own branch while that pull request is open. It should be empty on master, holding no more than this comment and the heading. -->

# pull-request

* Deliver the plan's documentation item across the trees it did not name, where the user documentation and one spec now state the opposite of the shipped behavior.

Existing Issue: The plan's documentation step named two specs and the installed-runtime guidelines and the diff touched exactly those, so `documentation/user-documentation/advanced-agents/harness.md` still tells a reader that nothing restarts a dead browser and a later connection attempt simply fails, that janissary starts a headless Chromium for the tab, and that the globe flag always tells you whether there is a browser to connect to right now, and `product/specs/remote-server.md` still says a remote `-b` launch starts its own confined browser and its own scratch directory. Severity: 5/10

Existing Risk: 4/10 - A user reading the harness page after this change is told to report a failed connect and never retry, which is the opposite of what now works, and the remote spec describes a far side holding a browser and a scratch directory it no longer starts.

Proposal Risk: 2/10 - Prose only, and the risk is a sentence rewritten into a claim that is also wrong; grep both trees for the superseded phrases before and after so no other page keeps the old claim.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1201: correct the user documentation and the remote spec that now contradict the connect-triggered browser". In `documentation/user-documentation/advanced-agents/harness.md`, under "Giving a harness a browser": change "Janissary starts a headless Chromium for that tab and hands the harness two environment variables" to say the endpoint and the two variables are published at launch and the Chromium starts on the harness's first connect, which is why a `-b` tab that never drives a browser never starts one; change "Nothing restarts it, and a later connection attempt simply fails" to say a later connect starts a fresh browser behind the same endpoint, with its own state, and that the dead browser's scratch directory is still kept for the post-mortem; and change "The 🌐 flag drops off the metadata row at the same moment, so the row never claims a browser that isn't there" to whatever the icon decision settled on. In `documentation/user-documentation/getting-started/tabs.md`, correct the metadata-row sentence claiming the 🌐 flag "always tells you whether there's a browser to connect to right now". In `product/specs/remote-server.md`, correct the paragraph beginning "The end-to-end browser moves it once more", which says the remote starts "its own protocol guard, its own confined browser, and its own scratch directory on its own host": it starts its own guard at spawn and its browser and scratch directory on the first connect from one of its own clients, which is the statement `product/specs/harness.md` now makes. Check `documentation/user-documentation/advanced-agents/remote-agents.md` and `documentation/user-documentation/advanced-agents/workspacing.md` for the same superseded claim and change only what is actually wrong. `ai/guidelines/sandbox-e2e-browser.md` and `product/specs/harness.md` were already corrected by this pull request and read correctly once the connect genuinely waits; leave their timing wording alone apart from the close-reason wording the security finding changes. Before and after, grep `documentation/` and `product/specs/` for "Nothing restarts", "simply fails", "always tells you", and "starts a headless Chromium" so no other page keeps the old claim.


* Correct the pull request description's and the specs' claim that a replacement browser gets a new internal address, when the address behind the guard is one fixed value for the tab's life.

Existing Issue: The description says the browser that replaces a dead one comes up with a "new internal address underneath" and `product/specs/harness.md` says "a new internal address under the guard", but `upstreamOf` in `src/browser/e2e-server.ts` builds the same browser port and the same `internalPath` token for every generation of a tab, which the new lazy suite pins by expecting the second connect to return the first connect's URL unchanged. Severity: 3/10

Existing Risk: 3/10 - A reader judging the containment story from these sentences believes each browser sits behind a fresh unguessable path, so a later change that keeps one private address for a tab's life looks like a regression when it is what already ships.

Proposal Risk: 1/10 - Sentence-level corrections, and the accurate claim — one private address serving every browser in a tab's life, with the process behind it changing — is the safer one to have written down.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1201: correct the claim that each replacement browser gets a new internal address behind the guard". In the pull request description, the "What" paragraph and the second "Behavior examples" block both say the replacement comes up behind a "new internal address underneath"; replace that with the accurate statement, that the same published endpoint, guard port and path, and private port and path serve every browser in that tab's life, and that a death changes the process, its scratch directory and the Chromium behind it. In `product/specs/harness.md`, the new paragraph beginning "Because the endpoint is minted at launch" says "a new internal address under the guard"; say instead that the private address behind the guard belongs to the tab for as long as the tab is open and the replacement is a different process behind it. In `product/specs/sandbox.md`, the protocol-guard paragraph says the browser's address "is named per connection rather than fixed, so that endpoint can outlive every browser behind it": the guard does ask for the live upstream on each client, but the value it gets back is fixed for the tab, so rewrite the clause to say the guard asks per client rather than pairing one upstream at startup, and that the private address itself never changes for the tab's life. Apply the same correction to design decision 4 of `product/plans/complete/workspaced-harness-connect-triggered-browser.md`, which promises the next connect a "fresh internal upstream", since that plan ships inside this pull request and would otherwise be the record of a behavior nobody implemented. The case in `src/browser/e2e-server-lazy.test.ts` asserting the second connect returns the first connect's URL is the evidence for all four corrections and must keep passing.


* Close the technical-debt gap the diff creates by leaving the eager browser start exported, with two start sequences in the file and one caller between them.

Existing Issue: `startE2EBrowserServer` in `src/browser/e2e-server.ts` survives in full, with its own guard options, its own session wiring and two dedicated suites, but its only production caller is gone — `harnessSpawnEnv` in `src/harness/scratch-dir.ts`, and through it `RemoteProcesses.spawnPty` in `src/remote/serve-processes.ts`, now reach the lazy builder — so a second implementation of the same acquisition sequence is kept alive by nothing but its own tests. Severity: 4/10

Existing Risk: 4/10 - The two sequences share only `portsOrReport` and `browserEnv`, so a fix to the guard options, the scratch allocation or the child's launch has to be written twice, and one written into only the uncalled copy leaves a suite passing against a path nothing runs.

Proposal Risk: 3/10 - Collapsing the eager path into the lazy one makes the launch and lifecycle suites asynchronous and touches every case in them, so the risk lands on the tests rather than on behavior.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1201: collapse the eager browser start into the lazy one so the file keeps a single acquisition sequence". Make the lazy builder the only implementation in `src/browser/e2e-server.ts` and give the eager entry point an explicit kick: `startE2EBrowserServer` becomes `startLazyE2EBrowserServer` followed by a `void ensureUpstream(lazy)` whose rejection is swallowed, because the failure already reports through `onGone` and ends the client, and a launch must never throw at a caller that is part-way through building a tab. Delete the eager sequence's own `newSession`, guard and spawn block once the kick covers it, leaving `portsOrReport` and `browserEnv` shared as they are. Re-point `src/browser/e2e-server-launch.test.ts` and `src/browser/e2e-server-lifecycle.test.ts` at the lazy builder through a helper that starts a browser before a case runs: the cases reading the guard options, the internal port and path, the child's argument vector and environment keep their assertions, and the ones that are synchronous today become async. If a caller outside this tree turns out to need the eager entry point, say so in its doc comment instead of leaving it looking equivalent, and pin both entry points from `src/harness/scratch-dir.test.ts`. The new connect-is-the-request cases in `src/browser/e2e-server-lazy.test.ts` and the guard's buffering and refusal cases in `src/browser/e2e-guard.test.ts` must keep passing untouched. What must not survive the change is two places in the file that independently allocate ports, mint path tokens, start a guard and spawn a child, because that is the duplication the next fix to this path will land in only one copy of.


* Keep a janissary-side filesystem path out of the close reason the confined agent reads, closing the security gap the launch-failure reason opens across the guard's trust boundary.

Existing Issue: `launchFailure` in `src/browser/e2e-guard.ts` puts the raw text of a start failure into the close frame, and the failures that reach it come from the scratch allocation and the child spawn, so a filesystem or spawn error hands an agent inside a workspace the janissary installation root, the browser scratch path and the host account's home directory in the one string `ai/guidelines/sandbox-e2e-browser.md` tells it to read. Severity: 3/10

Existing Risk: 3/10 - A confined agent learns the host account name and the shape of the installation it is denied, through a channel whose every other reason is a fixed phrase from `src/browser/e2e-frame-filter.ts` and where no such detail is needed to decide anything.

Proposal Risk: 2/10 - The agent loses the one diagnostic it had, which failure occurred, and falls back on the notification line and the band the human reads, which already carry the same account in full.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1201: keep filesystem paths out of the launch-failure close reason the guard sends to the agent". Replace the body of `launchFailure` in `src/browser/e2e-guard.ts` with a fixed phrase in the guard's own vocabulary — `e2e browser failed to start` — and let the full account reach the human through the report that `stopSession` already composes with `withChildOutput` in `src/browser/e2e-session.ts`. Keep the length bound, since a websocket close frame is still capped at 123 bytes, and keep the truncation as a guard against a future caller that passes something long. Update the two sentences that promise the client a reason: the first-connect paragraph in `ai/guidelines/sandbox-e2e-browser.md` should say the close reason says the browser did not start and that the human's notifications tab carries the detail, and the "or reports why it could not" clause in `product/specs/harness.md` should say the same. In `src/browser/e2e-guard.test.ts`, change the case asserting a rejected supplier's reason to expect the fixed phrase, and add a case whose supplier rejects with an `EACCES` error naming a path, asserting that no part of that path reaches the close frame. The filter's own reasons are the precedent to follow: short phrases, no filesystem detail, and the same answer shape whatever the client did.


* Bound how often a tab's browser is restarted and reported, handling the repeat-failure loop that connect-triggered restarts open where a death used to be final.

Existing Issue: Every lazy browser generation is its own session carrying the tab's `onGone`, so a launch that fails the same way every time, or a browser that crashes each time it comes up, spawns a fresh child and delivers a full death report — a notification, a new log file, a rewritten band, and a kept scratch directory — on every connect, with no backoff or limit, while `E2EBrowserOptions.onGone` still documents itself as invoked once. Severity: 5/10

Existing Risk: 5/10 - An ordinary Playwright script that retries its connect, or a confined agent that can crash its own browser, turns one broken browser into an unbounded stream of Chromium spawns, notifications, log files and scratch directories on the host outside the sandbox, where before this change the first death ended it.

Proposal Risk: 2/10 - A restart budget has to tell a browser that genuinely keeps failing apart from one that crashed once after long use, so a threshold set too tight refuses a legitimate second browser and one set too loose still lets a loop run for a while.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1201: bound connect-triggered browser restarts and their death reports so a failing browser cannot loop". In `src/browser/e2e-server.ts`, give the `LazyBrowser` record a restart budget: count consecutive generations that ended with a report — a throw from `startBrowser`, and a generation whose child exits before it has been up for a fixed minimum interval (and, once the readiness fix recorded above lands, a readiness wait that rejects) — and reset the count when a generation outlives that interval. Once the count reaches a small fixed limit, `ensureUpstream` rejects with a fixed phrase such as `e2e browser will not be restarted` without calling `startBrowser`, and the tab session delivers one last report through `onGone` saying the browser has stopped being restarted, so the human learns once rather than per connect; the guard keeps listening and `handle.close()` releases everything exactly as it does now. A single failure stays under the budget, so design decision 6's "a tab's first connect failing does not consume anything" still holds. Update the doc comment on `E2EBrowserOptions.onGone` in `src/browser/e2e-server.ts` to say it fires once per browser and once more when restarts stop, and the doc comment on `E2EBrowserHandle.close` there, which still says a close after a death is a no-op when for the lazy start it closes the guard and releases the ports. `reportBrowserGone` in `src/harness/browser-gone.ts` and the `browser-exited` path through `RemoteProcesses.spawnPty` in `src/remote/serve-processes.ts` need no change beyond receiving fewer calls. State the limit in the e2e section of `product/specs/harness.md` and in the "When it stops working" section of `ai/guidelines/sandbox-e2e-browser.md`, which currently promise that a later connect always starts a fresh browser. In `src/browser/e2e-server-lazy.test.ts`, add cases for a spawn that throws on every call — the connect after the limit rejects without calling `spawn` or `allocateBrowserScratch`, and `onGone` has been called the limit plus one times — for a child that exits immediately on every generation with the same outcome, and, with fake timers, for a generation that lives past the interval resetting the count so the next death restarts normally. The existing cases "consumes nothing, so the next connect starts a browser" and "reports the replacement dying as its own death, not as silence" must keep passing unchanged, since both stay under the budget.


* Correct the pull request description's error-path verification step, which describes a retry and a single report that the port-band failure and a repeating start failure do not produce.

Existing Issue: Step 6 of "How to verify" says to fill the browser port band so allocation fails at launch and then connect, expecting a close reason, one death report and a later connect that retries, but a full band at launch makes `portsOrReport` hand the harness no browser variables at all, so there is no endpoint to connect to and nothing ever retries for that tab, and a start failure that repeats is reported again on every connect rather than once. Severity: 3/10

Existing Risk: 3/10 - A reviewer or tester following the step either cannot perform it, because the connect it asks for has nowhere to go, or sees one report per connect and concludes the change is broken when it is behaving as written.

Proposal Risk: 1/10 - A description-only correction, and the risk is rewriting it against the restart-budget change above before that lands, which would make it wrong a second time.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1201: correct the description's error-path verification step for a full port band and a repeating start failure". Rewrite step 6 of the "How to verify" section in the pull request body as two steps. The first is the full band at launch: `portsOrReport` in `src/browser/e2e-server.ts` reports once through `onGone` and `startLazyE2EBrowserServer` returns an empty `env`, so the check is that the harness launches with neither `JANISSARY_BROWSER_WS_ENDPOINT` nor `JANISSARY_PLAYWRIGHT` set, one notification is delivered, and no guard is listening — the lazy suite's case "reports a full band through onGone and hands back no browser variables" in `src/browser/e2e-server-lazy.test.ts` pins exactly this. The second is a start failure after launch, such as an unwritable browser scratch directory: the connect is closed, a report is delivered for that attempt, and a later connect tries again and reports again if it fails again. Word the close-reason expectation to match whatever the security finding above settles for `launchFailure` in `src/browser/e2e-guard.ts`, and word the report count to match the restart-budget finding above if it has landed by then. No code or test changes; the step is corrected in the description only.
