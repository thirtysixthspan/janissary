<!-- This file is for maintaining work items tied to a pull request and lives on a pull request's own branch while that pull request is open. It should be empty on master, holding no more than this comment and the heading. -->

# pull-request

* Write the session record before parking a peer, so a detach can never leave a live remote session with nothing to reattach it by.

Existing Issue: `detach` in `src/sessions/actions.ts` resolves the record from the live channel's session id and, when `SessionsManager` is holding none, still parks the peer and answers `{ ran: true }` with no record — so a detach raised from a tab's metadata row, which reaches `SessionsManager.detach` through `src/controller.ts` without ever composing the list whose `mirror` step is the only thing that writes records, and a detach on a remote agent tab whose lazily-created shell has never been spawned, for which `recordOf` in `src/sessions/snapshot.ts` deliberately writes nothing, both drop the transport without `finish()` and leave the far side running with no local record, no row, and no reattach path. Severity: 9/10

Existing Risk: 8/10 - The user presses the one control this feature added to every remote tab, watches the tabs close as promised, and the session is gone: the peer holds its workspace and its processes on the remote host for the full seven-day expiry while janissary can neither list it nor reach it, which is exactly the invisible-infrastructure failure the feature exists to end, and the only recovery is an ssh session and a manual kill.

Proposal Risk: 2/10 - A detach that cannot be recorded is refused rather than silently lossy, so a user whose session genuinely has nothing recordable now sees a control that does nothing when pressed and needs a reason on the row or in the feed to explain why.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: detaching a session janissary holds no record of parks an unreachable peer". In `src/sessions/manager.ts`, make the record current before the action runs rather than only when the list is composed: extract the `mirror` step so `detach` (and the `act` path generally) refreshes the record for the entry it is about to park, and in `src/sessions/actions.ts` treat a still-absent record as a refusal — return `REFUSED` instead of `{ ran: true }` — so `RemoteManager.detach` in `src/remote/manager.ts` is never called for a session that cannot be listed afterwards. The second half is `recordOf` in `src/sessions/snapshot.ts`, which returns nothing when the channel has spawned no process: decide deliberately whether a remote agent tab with a live ACP session but no shell is recordable, and if it is, record it from the entry's workspace and label rather than from `spawnedProcesses()` alone, with `restoreSessionTabs` in `src/sessions/restore-tabs.ts` tolerating a record whose process list is empty. `src/sessions/manager.test.ts` currently masks this — its detach cases call `sessions.view()` first, which is what writes the record — so add cases that detach without composing the list, one for a metadata-row detach and one for a channel with no spawned process, and assert `loadRemoteSessions()` afterwards; the existing ordering case (`takes the entry out of the table before closing any tab`) pins the order that must not move.


* Correct the description's claim that the record mirrors what is live with no window a crash could land in, or make the mirror run when the live set actually changes.

Existing Issue: The pull request body states the record "is written as a *mirror* of what is live rather than through a separate 'remember this' step, so a session becomes reattachable the moment it has a workspace and something running in it, and there is no window a crash could land in", but `SessionsManager.mirror` in `src/sessions/manager.ts` runs only from `view()`, `view()` runs only when the sessions topic is read, and the only emitter of the `sessions` bus channel is `SessionsManager.changed` — which the four row actions fire and nothing in `RemoteManager`'s open, attach, release, close, or `channelClosed` paths does — so a remote session launched while the tab is shut, or launched after the tab was last redrawn, is never recorded at all, and the open list never notices a launch, a join, a close, or a transport loss until the user presses Refresh. Severity: 7/10

Existing Risk: 7/10 - The window the description says does not exist is in practice the whole session: a crash or a lost network with the sessions tab closed leaves a peer on the far host that `--relaunch` cannot restore and the list cannot show, and a user who does keep the tab open reads a list whose `provisioning`, `active`, and `reconnecting` states never change, which is worse than no list because it looks live.

Proposal Risk: 3/10 - Mirroring on channel lifecycle turns a read-driven write into an event-driven one, so a launch storm writes the record file several times in quick succession and the `sameRecord` comparison in `src/sessions/manager.ts` becomes the only thing keeping that from being a write per frame.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the remote-session record is only written when the sessions tab is composed". Emit `messageBus.emit('sessions', { type: 'changed' })` from the points in `src/remote/manager.ts` where the live set actually moves — the end of `open`, `attach`, `release`, `close`, `detach`, and `channelClosed` — and from `Reattach`'s `lost`/`accepted` transitions in `src/remote/reattach.ts`, then have `SessionsManager` subscribe to that channel and run its `mirror` step on it rather than leaving the write a side effect of `view()`; keep the subscription released in `dispose()`, which already exists. Check the frequency claim in `src/bus.ts`'s comment for the channel still holds after the change — these are per-channel events, not per-byte ones, so it should, but say so in the comment. Add cases to `src/sessions/manager.test.ts` asserting that a live entry appearing without any call to `view()` produces a record on disk, and to `src/plugins/topics.test.ts` that the subscription fires for a lifecycle event rather than only for an action; `src/remote/manager.test.ts` covers the lifecycle paths being instrumented and must keep passing unchanged.


* Deliver the plan's reattach half of the metadata-row control, which no call site can currently reach.

Existing Issue: `remoteSessionControl` in `web/src/shared/remote-session-control.ts` computes its state as `provisioning ? 'provisioning' : 'active'` and is the only producer of the prop, so `RemoteSessionButton`'s `reconnecting` branch in `web/src/shared/RemoteSessionButton.tsx`, the `reattach` arm of `ControllerCore.remoteSession` in `src/controller.ts`, and `SessionsManager.reattachTab` in `src/sessions/manager.ts` are all unreachable from the running application, while decision 24, `product/specs/sessions-tab.md` ("detach while the session is healthy, reattach while its transport is being retried"), and the pull request body all state the control offers both verbs; its `inFlight` prop is likewise declared on `AgentTabMeta` and never supplied by any of the four call sites. Severity: 5/10

Existing Risk: 5/10 - The one gesture that explains an unresponsive remote tab — press this to stop waiting out the backoff — is documented in a shipped spec and absent from the product, so a user with a reconnecting tab sees a detach control instead and parks a session they were trying to recover; and because the component's tests drive it with a hand-written `reconnecting` state, the suite reports the behavior as covered.

Proposal Risk: 2/10 - The reconnecting state has to reach the client over the tab view, so the control now depends on a field that can go stale if the server forgets to mark a tab when its channel recovers, showing reattach on a tab that is already live.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the metadata row's reattach control is unreachable because no call site produces the reconnecting state". Carry the channel's reconnect state to the client: `RemoteTarget` in `src/tab/types.ts` is what `AgentTabMeta` already receives for the host chip, so add the reconnecting flag there (or beside it on the tab view), set it from `entry.reconnect.active` wherever `RemoteManager` marks a lost transport in `src/remote/reattach.ts`, and clear it on `accepted`. Then have `remoteSessionControl` return `'reconnecting'` from that flag and pass `inFlight` from whatever the call sites in `web/src/harness/HarnessTab.tsx`, `web/src/agent-tabs/AgentTabBody.tsx`, `web/src/agent-tabs/InactiveAgentTabBody.tsx`, and `web/src/ShellTab.tsx` already know about a pending action, or delete the prop from `AgentTabMeta` and `RemoteSessionButton` if the component's own `pressed` state is the whole answer. `web/src/shared/AgentTabMeta.test.tsx` already pins the component's reconnecting behavior; the missing test is a wiring one, so add a case to `web/src/harness/HarnessTab.test.tsx` (or the agent-tab body tests) that a tab whose remote target reports reconnecting renders the reattach control, which is the assertion that would have caught this.


* Clear the metadata-row control's spinner when an action does not take the tab with it.

Existing Issue: `RemoteSessionButton` in `web/src/shared/RemoteSessionButton.tsx` sets `pressed` when an action is raised and never clears it, relying on the tab unmounting on a successful detach, so every outcome that leaves the tab open — a detach `RemoteManager.detach` refuses because the entry has no session id yet, the `reattach` verb once it is reachable, or a request the server drops, all of which are silent because `remoteSession` is an `ack` method with no answer wired back — leaves the control spinning and disabled for the life of the tab. Severity: 4/10

Existing Risk: 4/10 - A user whose detach was refused is left with a permanently spinning, permanently disabled control and no message anywhere, so the tab reads as mid-operation forever and the only way back is to close and reopen it; the same shape hides every future failure of this control rather than reporting it.

Proposal Risk: 2/10 - Clearing on an answer means a slow but successful detach can un-spin a moment before its tabs close, so the control briefly looks pressable again on a session that is already going.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the remote session control spins forever when its action does not close the tab". Give the action an outcome to clear on: either change `remoteSession` in `src/client-message.ts` from `ack` to `result` and have `ControllerCore.remoteSession` in `src/controller.ts` return what `SessionsManager.detach`/`reattachTab` already answer (both return a boolean), with `remoteSessionControl` in `web/src/shared/remote-session-control.ts` resolving a promise the button awaits; or keep `ack` and clear `pressed` when the tab's remote state changes, which is the same signal the previous entry's reconnecting flag provides. Report a refusal rather than swallowing it — a refused detach should reach the notifications feed through `notify` in `src/notifications.ts` the way the successful one does in `src/sessions/actions.ts`. Add cases to `web/src/shared/AgentTabMeta.test.tsx` beside the existing `spins and refuses a second press once an action is in flight` case: one that a refused action returns the control to its pressable state, and one that a reattach clears the spinner without the tab unmounting.


* Keep a parked session's row on screen while an end attempt is in flight instead of making it disappear.

Existing Issue: `endParkedSession` in `src/sessions/end-session.ts` opens a `RemoteManager` entry under the synthetic label `end-session:<session>` with the record's session id set as the channel's, and `SessionsManager.snapshot` in `src/sessions/manager.ts` treats every live entry's session id as live and filters the matching record out of `detached`, while `channelOf` in `src/sessions/snapshot.ts` finds no tab for that label and yields a member-less group that `composeSessionRows` drops — so pressing End removes the row entirely for as long as the attempt lasts, and `product/specs/sessions-tab.md` says the opposite for the failure case ("the row stays parked with its failure reported"). Severity: 4/10

Existing Risk: 4/10 - The user confirms a destructive action and the row vanishes with no indication anything is happening, so a slow or unreachable host reads as a completed end; minutes later the row reappears holding a workspace the user believes was destroyed, and in the meantime nothing stops them pressing End again on a second copy of the row or opening a second ssh connection to the same peer.

Proposal Risk: 2/10 - Keeping the row means the session is both recorded and live at once for the duration, so whatever marks it must be cleared on every exit from the end path or the row is left permanently showing an attempt that finished.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: a parked session's row disappears while its end attempt is running". Stop the end channel from counting as a live session: have `endParkedSession` in `src/sessions/end-session.ts` tell `SessionsManager` which session ids belong to an in-flight end, and in `src/sessions/manager.ts` exclude those entries from the `live` set `snapshot()` builds so the record keeps producing its detached row, or equivalently skip entries whose sole label carries the `end-session:` prefix when composing `channels`. Show the attempt on the row rather than hiding it — the row view in `src/protocol/sessions.ts` already carries `failure`, so an analogous in-flight marker is a one-field addition that `SessionRowActions` in `web/src/plugins/sessions/SessionRowActions.tsx` can disable `end` and `reattach` on, which also closes the double-press path that currently leaks a second ssh connection because `RemoteManager.open` overwrites the entry under the same label. Add cases to `src/sessions/manager.test.ts` asserting the row survives an unresolved end and carries its failure afterwards, and to `web/src/plugins/sessions/SessionList.test.tsx` that an in-flight row's destructive buttons are not pressable; the existing end cases in `manager.test.ts` mock `endParkedSession` and must keep passing.


* Settle a reattach whose peer accepts and then never answers the session-state query.

Existing Issue: `askSessionState` in `src/remote/resume.ts` returns a promise resolved only by an inbound `session-state-result` — no timeout, and no resolution when the channel closes — so `settleAccepted` in `src/sessions/reattach.ts` awaits forever and `startSessionReattach`'s promise never settles, which the pull request body says cannot happen because "a version-14 peer recognizes neither frame, so the mismatch is refused at the handshake"; the handshake is answered by the freshly started `janus remote-serve` that relays into the parked peer (`relayPeer` in `src/remote/serve-detach.ts`), not by the parked peer itself, so a remote host whose `janus` was upgraded while a session sat detached announces version 15 and then hands the query to a version-14 process that refuses it by name. Severity: 5/10

Existing Risk: 5/10 - The reattach hangs with its placeholder tab open and its record showing neither a failure nor a trash button, so the row offers a button that will hang again every time it is pressed, and each attempt leaves another unresolved promise and another ssh connection behind; the same hang is reachable without any version skew whenever a peer accepts and then stops answering.

Proposal Risk: 2/10 - A bounded wait can fire on a peer that was merely slow, reporting a failure for a session that is about to come back and leaving the row detached while its tabs are open.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: an accepted reattach hangs forever when the peer never answers session-state". Give `askSessionState` in `src/remote/resume.ts` two exits besides the answer: a bounded deadline, and resolution when the entry closes — `answerSessionState` already owns the `entry.sessionState` resolver, so clearing and rejecting it from `terminateRemoteEntry` and `detachRemoteEntry` in `src/remote/reattach.ts` covers the transport case. Have `settleAccepted` in `src/sessions/reattach.ts` map either exit to `{ kind: 'failed' }` so the reason reaches the row through the existing `apply` path in `src/sessions/actions.ts` and the trash button is earned as `product/specs/sessions-tab.md` describes. Then correct the version paragraph in `src/remote/protocol.ts` and the matching passage in `product/specs/remote-server.md`: state that the handshake check binds the relaying process and that a peer parked across a remote upgrade is caught by the timeout instead, since the current text promises a guarantee the relay architecture cannot give. Add cases to `src/sessions/manager.test.ts` or a new `src/sessions/reattach.test.ts` for an accepted reattach that never answers and for one whose channel dies mid-query; `src/remote/serve.test.ts`'s `session-state` cases cover the answering side and must keep passing.


* Narrow the sessions topic actions to the row that offers them, so a plugin cannot close or focus an unrelated tab that happens to share a recorded label.

Existing Issue: `actOnSessions` in `src/plugins/topics.ts` admits `focus`, `close`, and `detach` whenever `SessionsManager.holds({ label })` is true, and `holds` in `src/sessions/manager.ts` returns true when *any* row carries that label — including a `detached` or `ended` row, whose `label` is a recorded name belonging to no live tab — so a plugin that declares the `sessions` topic can send `{ topic: 'sessions', action: 'close', label: '<recorded label>' }` and `SessionsManager.close` will resolve it through `TabManager.findIndex` and close whatever tab currently bears that name, even though the row it was authorised by offers only `reattach`. Severity: 5/10

Existing Risk: 5/10 - The grant is wider than the list that motivates it, which is the property `src/plugins/api.ts` states in the topic's own comment: any plugin holding the `sessions` topic can close a user's tab by name as soon as a record with that name exists, and since recorded labels are ordinary harness names like `claude` the collision is likely rather than contrived, with a closed agent tab and its unsaved terminal state the visible result.

Proposal Risk: 2/10 - Checking the row's own action list moves the authorisation decision to data the manager composes on every call, so a row whose `actions` are computed wrongly in `src/sessions/rows.ts` now silently refuses a legitimate press instead of performing it.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the sessions topic lets a plugin close any tab whose label appears in a recorded row". Replace the `holds` predicate in `src/sessions/manager.ts` with one that takes the verb as well as the target and answers true only when a row both matches and lists that verb in its `actions`, then have every arm of `actOnSessions` in `src/plugins/topics.ts` pass its own verb; keep the two addressing modes as they are, since `reattach`, `end`, and `forget` are already keyed by session id. While there, make the label arms resolve the tab through the row they matched rather than by a second `findIndex` lookup, so the tab acted on is the tab the row named. The client-side check in `src/plugins/sessions/activate.ts` (`actOnRow`'s `row.actions.includes(intent)`) already applies this rule and stays as defence in depth — the host must not depend on it, since any plugin declaring the topic reaches `actOnSessions` directly. Extend `src/plugins/topics.test.ts`, which already covers refusal of an action naming an unknown row, with a case that an action naming a row that does not offer it is refused, and add a `src/sessions/manager.test.ts` case that a `close` aimed at a detached row's label leaves a live tab of the same name open.


* Share one confirm dialog between the plugin lists instead of shipping a second verbatim copy of it.

Existing Issue: `web/src/plugins/sessions/ConfirmSessionDialog.tsx` reproduces `web/src/plugins/conversations/DeleteConversationDialog.tsx` line for line — the same `selected` state, the same `actionsRef` key table, the same capture-phase `keydown` listener on `globalThis` that calls `preventDefault` and `stopPropagation` on every key, and the same markup against the host's modal CSS classes — differing only in that the title and confirm label arrive as props, so the two files are one component and a fix to either (the swallow-everything key handler being the obvious candidate) now has to be made twice. Severity: 3/10

Existing Risk: 3/10 - The next plugin list copies whichever version it finds, and the keyboard behavior of janissary's modal confirmations drifts apart per plugin — one dialog answering Escape and another not — with nothing in the tree that would show the divergence.

Proposal Risk: 2/10 - A shared component under the plugin tree becomes a de facto second API surface for plugins, so a change to it affects every list at once and the plugin boundary has to be explicit about what is shared and what is host UI.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the sessions confirm dialog is a verbatim copy of the conversations one". Promote the component to a shared plugin-side module beside `web/src/plugins/shared.css`, which already establishes that the plugin tree carries shared presentation without importing host UI — a `web/src/plugins/shared/ConfirmDialog.tsx` taking `title`, `confirmLabel`, `onConfirm`, and `onCancel` covers both call sites as written. Point `ConversationList` in `web/src/plugins/conversations/ConversationList.tsx` and `SessionList` in `web/src/plugins/sessions/SessionList.tsx` at it and delete both copies. Check `ai/guidelines/react-code-organization.md` and `ai/guidelines/plugins.md` first for where shared plugin presentation is meant to live and follow that if it names somewhere else. The conversations dialog's existing tests pin the keyboard contract and must keep passing against the shared component; move the sessions confirmation cases in `web/src/plugins/sessions/SessionList.test.tsx` to assert the wiring rather than re-testing the dialog's keys.


* Hold replayed frames only for the reattach that needs them, so an ordinary channel stops accumulating output for ids nobody will claim.

Existing Issue: `SessionRouter.output` and `.exit` in `src/remote/channel-sessions.ts` route any frame whose id has no listener into `PendingFrames`, unconditionally and for the whole life of the channel, and `discardUnclaimed` is called from exactly one place — `restoreSessionTabs` in `src/sessions/restore-tabs.ts` — so a channel that is not reattaching accumulates up to the 1 MB budget of output for ids whose listener has gone (a closed tab whose far side is still writing before its `kill` lands), re-encoding every held frame on each `claim` to recompute the byte total, and sets the `dropped` flag that makes the next unrelated `attach` report the truncated-replay line to the user. Severity: 4/10

Existing Risk: 4/10 - A long-lived remote channel grows a megabyte of output that will never be delivered and reports `Some remote output produced while disconnected was dropped to limit memory use.` into a tab that was never disconnected, which is a message the user cannot act on and cannot distinguish from the real one it borrows.

Proposal Risk: 2/10 - Holding only while a reattach is in flight means a frame arriving a moment after the window closes is dropped as before, so the window's start and end have to bracket the whole reattach rather than just its query.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the channel holds output for unclaimed ids on every channel, not only during a reattach". Gate the hold on the state that motivates it: `RemoteChannel` in `src/remote/channel.ts` already knows it is `reattaching`, and `RemoteManager.open` in `src/remote/manager.ts` knows a `resume` was supplied, so pass that through to `SessionRouter` and have `output`/`exit` fall back to today's drop when no reattach is settling. Bound the window at both ends — `restoreSessionTabs` already closes it — and scope `overflowed()` in `src/remote/channel-pending.ts` so the truncation line is reported only for the attach that actually lost frames. While there, keep a running byte total rather than re-encoding the whole buffer in `claim`. The held-frame cases in `src/remote/channel.test.ts` cover the reattach path and must keep passing; add one that a live channel with no reattach in flight drops an unlistened frame and raises no truncation report.


* Release an adopted remote spawn id when its reattach does not produce a shell, so a later tab cannot bind to a stale one.

Existing Issue: `ShellManager.adoptRemoteShell` in `src/shell/manager.ts` parks a recorded spawn id in a map keyed by tab label, consumed only when `spawnFor` is next asked for that label's shell, and `startSessionReattach` in `src/sessions/reattach.ts` parks one before the tab exists — so a reattach that fails, or one whose tab is closed before any command runs, leaves the id in the map indefinitely, and because `uniqueLabel` frees the label the moment that tab closes, the next remote agent tab granted the same label binds its first shell to a spawn id belonging to a process on a different channel instead of starting its own. Severity: 4/10

Existing Risk: 4/10 - The failure is silent by construction: the shell adapter registers under an id the far side never spawned, so the tab accepts commands and shows nothing back, and the user sees a remote agent tab that has simply stopped responding with no error anywhere to attribute it to.

Proposal Risk: 2/10 - Releasing on tab close means a reattach whose tab is torn down and rebuilt within the same flow loses its adoption and starts a second shell on the far side beside the one still running there, which is the behavior the adoption exists to prevent.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: an adopted remote spawn id outlives its reattach and can be claimed by a later tab". Give the adoption a release: `ShellManager` in `src/shell/manager.ts` should drop a label's adopted id when that tab closes, alongside whatever `MANAGER_TAB_RELEASE` in `src/managers.ts` already routes there, and `startSessionReattach` in `src/sessions/reattach.ts` should drop it on every non-`reattached` outcome rather than only on success. Key the entry by more than the label if the label alone cannot be made safe — the record in `src/sessions/store.ts` carries the session id, so storing the channel's session id beside the spawn id and refusing to adopt when the tab's current channel does not match closes the reuse path outright. Add cases to `src/shell/manager.test.ts` that an adopted id is gone after the tab closes and that a tab on a different channel does not adopt it; the existing remote-shell case covering `rsh…` id minting is what pins the un-adopted path and must keep passing.


* Deliver the reporting contract the pull request and its spec state on the reattach paths that end a session, which today record two notification lines.

Existing Issue: An accepted reattach whose peer is holding nothing, and a reattach the peer refuses, each lands two lines in the notifications feed — the `<what> on <host> ended.` line the actions in `src/sessions/actions.ts` report through the new `remote-session` event, plus the pre-existing `Remote janus on <host> ended — start a new agent or shell to continue.` line that `terminateRemoteEntry` announces on both of those paths (directly from `handleReattachResult` in `src/remote/resume.ts` on a refusal, and via `RemoteManager.close` in `src/remote/manager.ts` from `settleAccepted` in `src/sessions/reattach.ts` when the peer came back empty) — while `product/specs/sessions-tab.md` states each action records one line and the pull request body says a notification names it. Severity: 3/10

Existing Risk: 3/10 - A user recovering a parked session reads two differently worded endings for one event, one of which recommends starting a new agent or shell while the sessions tab is telling them the session ended, so the feed teaches the user its lines cannot be taken at their word.

Proposal Risk: 2/10 - Suppressing the generic announcement on the sessions-driven paths means a session that ends on its own at the same moment as a reattach would report neither line, so verification has to check the row's ended state and the feed together.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: a reattach that ends a session records two notifications where the spec promises one". Make the generic `remote-session-ended` announcement conditional on who ended the session, keeping `src/sessions/actions.ts` as the single narrator for the sessions tab since its `remote-session` line is the one `product/specs/sessions-tab.md` defines: `terminateRemoteEntry` in `src/remote/reattach.ts` already takes an announce flag, so thread a resume-aware one through `handleReattachResult` in `src/remote/resume.ts` — the `state.resuming` flag there already distinguishes a pressed reattach from the automatic reconnect, and a refusal on the automatic path should keep announcing as it does today — and have `settleAccepted` in `src/sessions/reattach.ts` call the close for the empty-peer case with announcing suppressed, composing with the earlier entry's change to `RemoteManager.close` rather than duplicating it if that lands first. No spec rewrite is needed once the code matches `product/specs/sessions-tab.md`'s reporting section as written, but check that `product/specs/notifications.md`'s description of `remote-session-ended` still holds for the automatic path. Add cases asserting exactly one notification for a refused reattach from a record and for an accepted reattach that reports nothing, in `src/sessions/manager.test.ts` or beside the reattach-from-record cases in `src/remote/manager.test.ts`; the announce-flag cases in `src/remote/reattach.ts`'s existing tests and the wording pins in `src/notifications.test.ts` must keep passing.


* Give the metadata row's detach confirmation the keyboard handling its sibling dialog on the sessions tab already has.

Existing Issue: The confirmation `RemoteSessionButton` in `web/src/shared/RemoteSessionButton.tsx` renders for a detach raised from a remote tab's metadata row is bare modal markup — nothing is focused into it and no keydown listener exists, so Escape cannot cancel, Enter cannot confirm, and a keyboard user who opens it has no way out but the mouse — while `ConfirmSessionDialog` in `web/src/plugins/sessions/`, shipped by the same pull request for the same feature's other front door, implements the full y/n/Enter/Escape/arrow contract on the same host modal CSS classes. Severity: 3/10

Existing Risk: 3/10 - The one destructive action every remote tab carries opens a dialog a keyboard-only user cannot answer, and because the two front doors of the same detach behave differently, the next contributor has no single contract to preserve and will copy whichever version they find.

Proposal Risk: 1/10 - The dialog gains the keyboard behavior its sibling already pins in tests, but a capture-phase listener that swallows every key while open means a dialog rendered on top of another one at the same moment needs checking.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: the metadata row's detach confirmation cannot be answered from the keyboard". Bring the confirmation in `web/src/shared/RemoteSessionButton.tsx` up to the contract `web/src/plugins/sessions/ConfirmSessionDialog.tsx` implements: move focus into the dialog on mount, add the capture-phase keydown listener answering y, n, Enter, Escape, and the arrow-key toggle between the two buttons, and highlight whichever one is selected. The plugin-side component sits across the plugin import boundary and cannot be consumed by host code, so either extract the shared shape into a host-side module under `web/src/shared/` that both render, or implement the contract here deliberately and say so in the component's comment — check `ai/guidelines/react-code-organization.md` first and follow wherever it places shared presentation. Extend the remote-session-control cases in `web/src/shared/AgentTabMeta.test.tsx` with Escape cancelling, Enter confirming, and focus landing inside the dialog; the confirmation cases in `web/src/plugins/sessions/SessionList.test.tsx` pin the sibling contract and must keep passing.


* Attribute the sessions tab's notification lines to the sessions tab when it is open, as the plan specifies, rather than always to the active tab.

Existing Issue: Plan item 12 states the `remote-session` lines are "attributed to the sessions tab when it is open and to the active tab otherwise", but `report` in `src/sessions/actions.ts` always passes `managers.tab.cur().label` to `notify`, so with the sessions tab open but not focused, a detach raised from a metadata row is attributed to whatever tab the user happens to be reading. Severity: 2/10

Existing Risk: 2/10 - The feed's provenance header names a tab that did not produce the change, so a user scanning the feed later attributes a detach or an end to the wrong surface.

Proposal Risk: 1/10 - The attribution becomes deterministic, but a line about a session will now name the sessions tab even when the action was raised elsewhere, which is the plan's stated intent and is worth one sentence in `product/specs/sessions-tab.md`'s reporting section so the behavior is documented rather than incidental.

Proposal: Execute ./ai/tasks/work-an-issue.md "PR 1143: session action notifications are attributed to the active tab instead of the sessions tab". In `src/sessions/actions.ts`, resolve the attribution label once per report: the sessions plugin's tab when one is open — its label is what `TabManager.openPluginTab` derives from the manifest's `tabLabelPrefix` (`sessions`, in `src/plugins/sessions/manifest.ts`) and the plugin's fixed `sessions` instance key, resolved the way `NOTIFICATIONS_LABEL` is for the notifications feed in `src/notifications-tab.ts` — falling back to `managers.tab.cur().label` when it is not. Add a case to `src/sessions/manager.test.ts` that a detach with the sessions tab present but another tab active attributes the line to the sessions tab; the provenance formatting pins in `src/notifications.test.ts` must keep passing.
