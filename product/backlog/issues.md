# issues

## ready

* when a file is dragged into an edit tab, the full file path should be inserted into the editor at the current keyboard input position.

* clicking the resync button in the editor should trigger a resync with github, update the tab content accordingly, resolving conflicts if necessary.

## development

## deferred
