# issues

## ready

* create a default context menu that contains copy and paste mentu items. this menu should appear when the right mouse button is clicked, and no other context menu is defined for context. For example the file navigator already has a context menu that should be shown instead of the default. Because no right click context menu is defined in the editor tab, this default should show up.

## development

## deferred

## declined
