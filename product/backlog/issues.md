# issues

## ready

* in the file navigator, git synced directories should be shown with a git sync icon that when pressed will trigger a refresh against origin master and rendering of the tree. only the root of the tree git synced trees show this icon.

## development

## deferred
