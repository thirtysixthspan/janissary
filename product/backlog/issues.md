# issues

## ready

* opening the file navigator to a path that doesn't exist should not fail. instead it should poll the directory for eventual creation and show a looking for <path> message in the waiting time.

* the harness tabs should get the unread flag when the harness tab is hidden but the harness has completed work or is waiting on user input

## development

## deferred
