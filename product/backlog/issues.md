# issues

## ready

* permission prompt formatting needs update. the `● <time> <tab>` should be colored by the notifying tab. the rest should be the standard text color for the theme (e.g. white). The `view capture` should be a link color and look like an actionable button, including hover state. 

## development

## deferred
