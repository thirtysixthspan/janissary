# issues

## ready

* profile should be able to open editor tabs with git synced files. the git sync workspace will need to be created because editor tabs launched from a profile currently open empty files in a nonexistant workspace.

## development

## deferred
