# issues

## ready

* on a profile launch, the tabs should be ordered in the tab strip by number property. currently harnesses then editor tabs are opened. the tab number and group should both be respected when ordering the tabs in the tab strip.

* profile should be able to open editor tabs with git synced files. the git sync workspace will need to be created because editor tabs launched from a profile currently open empty files in a nonexistant workspace.

## development

## deferred
