# issues

## ready

* a single image may only be openned in a single image tab. opening an image already open in a tab will focus the pre-existing tab, but not open another.

* remove the buttonface styling on the git sync icon, making it just a clickable icon.

* when a git synced file is openned in an editor, the editor content is empty while syncing. When this happened the git sync button should animate to communicate syncing.

## development

## deferred
