# issues

## ready

* when a command is typed in the command bar, another tab is focused, then the original tag is focused again, the previous unexecuted command typed in associated with the tab should still be in the command bar ready to change or execute.

* when pasting text into an editor tab, the keyboard caret should be positioned at the beginning of the pasted text, and the scrolled window view should not change from that position it was in before the paste was executed.

## development

## deferred

## declined
