# issues

## ready

* injecting a command into the harness using the 'with' parameter should execute the command in the harness, so needs to include a carriage return at the end of the command.

* notifications tab should not show unread badge when in the sidebar.

## development

## deferred
