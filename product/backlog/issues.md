# issues

## ready

* when deleting a file from the file navigator, a confirmation dialog comes up. After an action on the dialog, the keyboard focus should return to the file navigator. If the file is deleted, focus the parent directory. if the deletion is canceled return the keyboard focus to the file.

* after resyning a git synced file and content has be3en removed from the file, the file content in the editor tab should be updated to remove the no longer present content.

## development

## deferred
