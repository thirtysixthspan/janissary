# issues

## ready

* in the editor tab, the keybindings for the query input line should be the same as in the text buffer, except for tab/enter which takes on a different behavior on the query line. for example, cmd+s should trigger the editor file to be saved. 

* in a question dialog, once an approve question is opened, the keyboard input focus should be on cancel. in a question dialog, once an ask question is opened, the keyboard input focus should be on the text input. tab should cycle between answers buttons in the question dialog arrow keys left and right should navigate the answer options as well. When exposing a tab when there is a question, the cancel button should get keyboard focus.  

* update /ai/tasks/improve-namespacing.md to recognize when a namespace already exists and move files appropriate to that namespace into the existing namespace. It can move any number of files into the namespace as long as they are related to code in the namespace.

## development

## deferred
