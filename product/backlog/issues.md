# issues

## ready

* a sandbox workspace should permit read access to the ai/ directory in the running janissary home directory so that the task picker can execute the tasks. the task picker should output execute $janissary/ai/tasks/work-an-issue.md where the $janissary will be implied to mean the home directory of the running janissary instance. this home directory should be provided to the workspaced agent. For example the running instance may be from a globally installed npm package.

## development

## deferred

## declined
