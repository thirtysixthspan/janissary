# issues

## ready

* when creating a new file in the file navigator it should not achieve this by injecting the command newfile into the last focused terminal.

* in a covnersation tab, when a response is streaming in, the page should scroll with the response unless the user scrolls into the backlog to review earlier content. Then the response should stream into the buffer without moving the scrolled position. Only if the user scrolls down to the end of the conversation up to that point should the automatic scrolling to the end of the content resume.

## development

## deferred

## declined
