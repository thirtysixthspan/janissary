# issues

## ready

* PR 952: remove Conversations from the metadata row of the conversations tab, move the new conversation button into the metadata row. format the content and buttons in the metadata row the same as the metadata row in other tabs like agent and harness. the new conversation button should be an icon like the other buttons.

* PR 952: In the Conversations tab, the list of conversations should be keyboard navigatable, with the current line highlighted, the coversation selectable with the enter key. the current line should move up and down with up and down arrows. a single click of the mouse should move the current line to the hovered conversation. it should require a second click to open that conversation in a new tab.

* when the the file navigator is docked in a sidebar, the metadata row should be two lines, where the second line are just the buttons and the first row is the rest.

## development

## deferred

## declined
