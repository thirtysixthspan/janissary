# issues

## ready

* when renaming a file in the file navigator, the input field should extend to the right edge of the file navigator. 

* after a new directory is created in the file navigator, the directory should be focused and rename triggered so that the input field will allow the user to edit the directory name.


## development

## deferred
