# issues

## ready

* add a flag to shell command to launch a shell with an interactive pty terminal.

* add an init command to create the ai and product directories in a new project recursively and .gitkeep the empty directories.  

## development

## deferred
