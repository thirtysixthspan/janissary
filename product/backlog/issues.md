# issues

## ready

* editing a file by clicking on it in the file navigator should launch the file relative to the root of the file navigator. if the navigator is showing a workspace the file edited should be relative to the root of the workspace. right now it appears as if the file is opened relative to the root of the currently focused tab in the main application area.  

## development

## deferred
