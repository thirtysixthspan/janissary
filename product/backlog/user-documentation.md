# user documentation gaps

Last run: 2026-07-23 21:10 UTC

Functional areas where user documentation lags application behavior, scored 1–10 for the size of the mismatch (10 = completely undocumented).

## candidates

* agent-questions (10/10) — Agent questions and approval requests have no user-documentation page and no `help.md` row. All 11 verified facts are missing, including the `question ask` and `question approve` syntax available to agents and humans, free-text versus option-button panels, non-modal behavior, focus and Tab/arrow navigation, one-pending-question queuing, notifications for questions raised in another tab, cancellation results, and in-memory lifetime. Add a dedicated page under `documentation/user-documentation/advanced-agents/` and add the command to the appropriate `help.md` row or command list. The ground truth is `product/specs/agent-questions.md`, `src/question-command.ts`, `src/commands/question.ts`, `src/questions.ts`, `src/notifications.ts`, and `web/src/QuestionPanel.tsx`.

* editor-tab (8/10) — The editor page covers opening, saving, syntax highlighting, and ordinary unsaved-close behavior, but 12 of 22 documentable facts are missing, including focus protection while clicking blank editor space or metadata, the independent unsaved-file behavior when several new tabs target the same path, tab-label renaming as an on-disk file rename, debounced transient draft sync to monitors, clean-buffer live reloads and dirty-buffer overwrite confirmation after external changes, and the in-editor persona-suggestion syntax, Ctrl/Cmd+Enter trigger, Tab completion, accept/decline blocking flow, status pills, and request-line cleanup rules. Fix the existing page `documentation/user-documentation/tab-types/editor.md`; no `help.md` row is needed. The ground truth is `product/specs/editor-tab.md`, `web/src/EditorTab.tsx`, `web/src/editor/useEditorSync.ts`, `src/editor/watch-manager.ts`, `web/src/editor/useEditorSuggest.ts`, `web/src/editor/PendingSuggestPanel.tsx`, and `src/editor-suggest/handler.ts`.

* profiles (8/10) — 4 of 12 facts are missing and 1 core fact is wrong: the existing page says `profile launch` restores an agent's transcript, history, and schedule, but `product/specs/profiles.md` and `src/profile/manager.ts` launch a fresh agent state from the profile template, while the page also omits the full `profile save <name>` behavior, bare-launch picker, bare-validation behavior, and profile layout reset semantics. The page should explain that profile launch restores configuration such as cwd and presentation but not live transcript/history, and should document save's captured and skipped tabs, single-file output, overwrite behavior, and the launch/validate edge cases. Fix `documentation/user-documentation/automation/profiles.md`; the `profile` command is absent from `help.md`, so add or correct its row there as part of the documentation fix. Ground truth is `product/specs/profiles.md`, `src/profile/manager.ts`, `src/profile-command.ts`, `src/profiles.ts`, `src/profile/agent-opener.ts`, `src/profile/save.ts`, `src/profile/save-entries.ts`, and `src/profile-file.ts`.

* embedded-web-page (6/10) — 1 of 14 facts is wrong: `documentation/user-documentation/tab-types/web-pages.md` says page-strip labels include the page number, but the shipped page tab title is the root domain alone; the page number remains the identifier for `close page <n>`. The rest of the current page covers the recent back/forward/reload controls, editable address, validation, and address/label following behavior. Correct the label description and example in the existing page, keeping its `open`, close, and navigation coverage aligned with `help.md`; the spec's label wording is stale and the app is the ground truth in `product/specs/embedded-web-page.md`, `src/openers/page.ts`, `src/tab/index.ts`, `src/tab/navigate.ts`, `web/src/PageTab.tsx`, and `web/src/TabItem.tsx`.

* application-config (5/10) — 1 of 8 documentable configuration facts is missing: `documentation/user-documentation/getting-started/startup.md` omits the `syncPaths` setting, whose default is `product/backlog/` and `product/plans/`, whose entries may be exact files, trailing-slash directory prefixes, or single-segment `*` wildcards, and which is edited directly in `.janissary/config.json` with no runtime command. Add the setting and its matching semantics to the existing configuration table, and link its editor-tab behavior; `help.md` needs no row. The ground truth is `product/specs/application-config.md`, `src/config.ts`, `src/sync-path-match.ts`, `src/open-file-manager.ts`, and `documentation/user-documentation/tab-types/editor.md`.

* file-tree-tab (4/10) — 3 of 27 documentable facts are missing: the file navigator now shows a GitHub button when its root is in a repository with a GitHub `origin`, the button opens that repository's current-branch commits page in an in-app page tab, and it is absent for non-GitHub remotes, missing remotes, or an undetermined branch. Add a short GitHub-button subsection to `documentation/user-documentation/tab-types/file-navigator.md`, including those availability and URL details; `help.md` needs no new command row. The ground truth is `product/specs/file-tree-tab.md`, `src/file-tree/git-refresh.ts`, `src/file-tree/manager.ts`, `src/github-url.ts`, `web/src/FileTreeHeader.tsx`, and `web/src/FileTreeGithubButton.tsx`.

* tabs (4/10) — 2 of 15 facts are missing and none are wrong: `documentation/user-documentation/getting-started/tabs.md` documents the file-navigator metadata button but omits the metadata-row **New agent here** button and the agent-only **Open transcript** button, including their working-directory behavior, group placement, no-op/notification edge cases, and transcript-to-editor result. Add those details to the existing metadata-row section; `help.md` does not need a new command or key-binding row. Ground truth is `product/specs/tabs.md`, `web/src/AgentTabMeta.tsx`, `web/src/AgentTabBody.tsx`, `src/profile/manager.ts`, `src/profile/new-agent.ts`, `src/controller/transcript.ts`, `src/controller.ts`, and `src/message-handler.ts`.

## unverified

## resolved
