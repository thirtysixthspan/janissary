# user documentation gaps

Last run: 2026-07-19 16:40 UTC

Functional areas where user documentation lags application behavior, scored 1–10 for the size of the mismatch (10 = completely undocumented).

## candidates

* application-themes (9/10) — Application themes have no doc page; only the terse `help.md` rows exist, leaving roughly 8 of 9 facts undocumented (the sibling syntax-theme setting is documented in `documentation/user-documentation/command-bar/commands.md` and `documentation/user-documentation/getting-started/startup.md`, but the app theme is not). Missing facts: `theme <name>` applies immediately with no restart and persists to the `theme` key in `.janissary/config.json`; the six built-in themes (`dark` default, `light`, `solarized-dark`, `solarized-light`, `nord`, `dracula`) with no custom themes; bare `theme` opens a swatch picker with arrow keys, `Return`, `Escape`, and click; names match case-insensitively and an unknown name lists the available themes; `theme sync` sets the syntax theme to the app theme's name when one exists; app theme and syntax theme are independent settings; embedded web pages and ANSI shell output are deliberately outside the theme. The ground truth is `product/specs/application-themes.md`. Fix by adding a page (likely under `documentation/user-documentation/getting-started/` near the startup configuration docs or in `command-bar/`), updating the config table in `documentation/user-documentation/getting-started/startup.md` to include the `theme` key, and verifying the `help.md` rows still match.

* browser (9/10) — The `browser` command has no doc page; only the terse `help.md` row exists, leaving roughly 12 of 13 facts undocumented. Missing facts: the full subcommand set (`open`, `list`, `use`, `goto`, `content`, `eval`, `shot`, `close`, `window close`); `--headed`/`-H` and the rule that the mode is fixed for the life of the tab's browser process; each tab runs its own browser process with isolated windows (`w1`, `w2`, …) and a current window; page actions auto-launch the browser and auto-open a window; `content` truncates at ~10k characters; `shot` writes a temp PNG and opens Preview on macOS; browser windows are not restored on `--relaunch`. The ground truth is `product/specs/browser.md`, `src/browser-command.ts`, and `src/browser.ts`. Fix by adding a page (likely under `documentation/user-documentation/command-bar/` or a new automation topic) and verifying the `help.md` row still matches; `documentation/user-documentation/command-bar/tab-completion.md` already covers browser subcommand completion and can be linked.

* connection (9/10) — The `connection` command has no doc page, and the `help.md` row is both terse and stale: it lists the kinds as sqlite/shell/acp/browser while the app also manages `ssh` connections, so 9 of 10 facts are missing and 1 is wrong. Missing facts: `connection list` output covering the five kinds and their scopes (sqlite and ssh are global, shell/acp/browser are per-tab); `connection close <kind>:<id>` semantics per kind, including that sqlite reopens on the next `db` command and shells respawn; ssh ids match label first, then destination; the floating connections panel and the web metadata-bar plug button with its hover, pin, and five-second auto-show behavior; tab-completion of close targets. The ground truth is `product/specs/connection.md`, `src/connections.ts`, and the connection cases in `src/commands/`. Fix by adding a page under `documentation/user-documentation/command-bar/`, correcting the `help.md` row to include ssh, and linking from `documentation/user-documentation/command-bar/tab-completion.md`.

* database (9/10) — The `db` command has no doc page; only the terse `help.md` row exists, leaving roughly 8 of 9 facts undocumented. Missing facts: the subcommands `db sqlite create <name>`, `db sqlite delete <name>`, `db sqlite query <name> <sql>`, and `db sqlite list`; only the `sqlite` engine is accepted; names must match `^[A-Za-z0-9_-]+$`; row-returning statements (`SELECT`, `PRAGMA`, `WITH`, `EXPLAIN`) print an aligned table with a row count while other statements report `OK.`; connections are global and persistent so transactions and TEMP tables survive between commands; database files live in `.janissary/db/sqlite/` and are never cleared, surviving quit and `--relaunch`. The ground truth is `product/specs/database.md` and `src/connections.ts`. Fix by adding a page under `documentation/user-documentation/command-bar/` and verifying the `help.md` row still matches.

* messaging (9/10) — The `msg` and `broadcast` commands have no doc page; only two terse `help.md` rows exist, leaving all 6 facts undocumented. Missing facts: the three message kinds and their semantics (`info` lands in the recipient's transcript and context, `request` executes and returns the output to the sender as a response, `command` executes with no response); the `i`/`r`/`c` kind aliases; the sender-side transcript record of what was sent; `broadcast <all|agent[,agent...]> <kind> <text>` with `all`/`*` targeting, sender exclusion, and the delivery report naming unreachable recipients. The ground truth is `product/specs/messaging.md` and the messaging command handlers in `src/`. Fix by adding a page under `documentation/user-documentation/command-bar/` and verifying the two `help.md` rows still match; `documentation/user-documentation/command-bar/tab-completion.md` already covers recipient completion and can be linked.

* monitoring (9/10) — Monitors have no doc page; only the three terse `help.md` rows (`monitor`, `unmonitor`, `monitors`) exist, leaving roughly 13 of 14 facts undocumented. Missing facts: `monitor <persona> [target...]` with inline mode versus reporting-tab mode and `group:<n>` targets; targets match display aliases case-insensitively; the reporting tab's metadata line with context size, reset button, and context-snapshot button; monitors receive full target transcripts, harness/ssh screen captures, editor live-buffer diffs, and page-tab visible text; the 30-second flush cycle; replies deliver via `[SUMMARY]:`/`[SUGGESTION]:`/`[COMMAND]:` markers and suggested commands are clickable to run in the target tab; `monitor ask <persona> <question>` for direct questions; suggestion rating; personas opt into `web_search`/`web_fetch` via a `[//]: # tools:` line; `unmonitor <persona>` / `unmonitor --all` and the lifecycle rules. The spec's lifecycle wording says `monitor stop`, but the app command is `unmonitor` — document the app's behavior. The ground truth is `product/specs/monitoring.md`, `src/commands/monitor.ts`, and `src/monitor/`. Fix by adding a page under `documentation/user-documentation/automation/` and verifying the three `help.md` rows still match.

* harness (7/10) — `documentation/user-documentation/advanced-agents/harness.md` is substantial but has drifted: 1 fact is wrong and about 4 of 18 are missing. Wrong: the page says `harness` with no name prints the `Usage:` message, but in the web app a bare `harness` now opens the New harness launch dialog (name, label, model, effort dropdown, workspace); only the terminal UI still prints `Usage:`. Missing: the `with <prompt>` clause that injects a launch prompt once the harness is running (also usable from `schedule`, e.g. `schedule deploy at 5pm harness claude with fix the failing tests`); `harness capture <name>`, which snapshots a harness tab's screen into an editor tab (present in `help.md` but not on the page); an explanation of `-y`/`--yes` auto-approval of claude permission prompts (the flag appears in the page's syntax line but is never explained — it is claude-only and requires `-w`); the metadata row showing the harness's model and effort. The ground truth is `product/specs/harness.md`, `src/commands/harness*.ts`, and `web/src/HarnessLaunchDialog.tsx`. Fix by correcting the bare-`harness` paragraph and adding the missing sections to `documentation/user-documentation/advanced-agents/harness.md`; check the `help.md` harness row still matches afterwards.

* sidebars (7/10) — Sidebar behavior is documented piecemeal in `documentation/user-documentation/tab-types/file-navigator.md`, `documentation/user-documentation/tab-types/notifications.md`, and `documentation/user-documentation/getting-started/tabs.md`, with 1 wrong fact and about 4 of 10 missing. Wrong: `file-navigator.md` says a sidebar can hold a tree *or* the notifications feed and that docking one where the other sits displaces it — but the app lets the two different kinds share one sidebar (confirmed in `src/tab/dock.ts`); only docking a tab of the *same* kind displaces the occupant. Missing: the sidebar's own tab strip with one entry per docked tab, click-to-switch, and per-entry close buttons; double-clicking the visible entry's label renames it inline like any tab; which docked tab is visible is client-side state that resets on relaunch; the `schedules` tab is a third dockable kind (`schedules left`/`right`). The ground truth is `product/specs/sidebars.md`, `product/specs/scheduling.md`, and `src/tab/dock.ts`. Fix by correcting the sharing paragraph in `documentation/user-documentation/tab-types/file-navigator.md` and mirroring the fix in `documentation/user-documentation/tab-types/notifications.md` and `documentation/user-documentation/getting-started/tabs.md`, adding the sidebar-strip behavior where sidebars are introduced.

* application-config (6/10) — The Configuration table in `documentation/user-documentation/getting-started/startup.md` lists only `transcriptMaxLines`, `tabNameMaxLength`, and `syntaxTheme` and claims "All three settings are editable", but the app reads at least three more keys from `.janissary/config.json`, so about 3 of 7 facts are missing and the "all three" framing is stale. Missing: the `theme` key (the application color theme, set by `theme <name>`); the `notifications` events object (`stateChange`, `incomingMessage`, `scheduleFire`, `agentStart`, `rateLimited`, each opt-in and only editable in the file — the notifications page alludes to this but the config table omits it); the `sandboxWorkspaces` toggle (documented in `documentation/user-documentation/advanced-agents/workspacing.md` but absent from the config table). Also missing: a runtime change (`theme`, `syntax theme`) rewrites `config.json` preserving other keys, and warns if the write fails. The ground truth is `product/specs/application-config.md`, `product/specs/sandbox.md`, and the config loader in `src/`. Fix by extending the Configuration table in `documentation/user-documentation/getting-started/startup.md` to the full key set and correcting the "all three" sentence.

* cli (6/10) — The `janus` command line is documented in `documentation/user-documentation/getting-started/startup.md`, but 4 of 13 facts are missing, including the only way to stop the app from a shell. Missing facts: `janus stop [<project-dir>]`, which reads the instance lock and sends the running server SIGTERM, printing a friendly message when nothing is running; a normal launch now detaches the server into the background and returns the shell prompt, so Ctrl+C in the launching terminal no longer stops the app; server output goes to `.janissary/log/server.log`, which is truncated on a normal launch and appended under `--relaunch`; under `--no-open` the launcher prints the server URL. The documented flags, usage errors, and troubleshooting entries in the page remain accurate. The ground truth is `product/specs/cli.md` and `bin/janus.mjs`. Fix by extending `documentation/user-documentation/getting-started/startup.md` with a stopping-the-app section and the detached-launch behavior; consider a `janus stop` mention in `documentation/user-documentation/getting-started/install.md` if it describes running the app.

* file-tree-tab (6/10) — `documentation/user-documentation/tab-types/file-navigator.md` is solid on opening, docking, git coloring, and keyboard navigation, but about 8 of 20 facts are missing, all from recently shipped interactions. Missing: the file-search pop-up and its match-path `>` prefix; the New file button and `Cmd+N`, including opening the next free name when the target exists; the click-drag-release gesture to move files between directories (dropping onto a file moves into its directory, with a cursor-following label while dragging) — the page documents the move *undo/redo* keys but never the drag gesture that creates a move; `Backspace`/`Delete` deleting the selected file or directory (the page's undo note says deletion is permanent but the key itself is not in the keyboard table); dragging a row into the command bar to insert its path. The ground truth is `product/specs/file-tree-tab.md` and `web/src/` file-navigator components. Fix by adding the missing rows to the Mouse and Keyboard tables and a short section on search and New file in `documentation/user-documentation/tab-types/file-navigator.md`.

* scheduling (6/10) — `documentation/user-documentation/automation/scheduling.md` covers the command grammar and per-tab schedule window well, but about 6 of 16 facts are missing, mostly whole recently-shipped surfaces. Missing: the `schedules` command (plural) that opens the aggregated view-only tab listing every timer across all tabs ordered next-to-run, with row selection, Enter to jump to the owning tab, and `Backspace`/`Delete` opening a delete-confirmation dialog; `schedules left`/`right` docking; the web New schedule dialog opened by a bare `schedule` (the terminal UI keeps the `Usage:` message); the metadata-bar clock button with hover, pin, and five-second auto-show; scheduling a harness launch with a prompt (`schedule deploy at 5pm harness claude with fix the failing tests`); a firing into a busy agent tab queues behind its other queued commands. The ground truth is `product/specs/scheduling.md`, `src/commands/schedules.ts`, and `src/schedule-manager.ts`. Fix by adding a schedules-tab section and a New-schedule-dialog note to `documentation/user-documentation/automation/scheduling.md`, and a `schedules` row to `help.md`'s Commands table if one is still absent.

* ssh-tab (6/10) — SSH tabs have no doc page of their own, but the SSH sessions section of `documentation/user-documentation/advanced-agents/harness.md` covers the basics (verbatim passthrough, labeling, `connection close ssh:<name>`, input/lifecycle by reference to harness tabs, no `as`/`-w`), so about 5 of 10 facts are missing, none wrong — rescored from 9 after the earlier run's filename-only check missed that section. Missing facts: the `Usage: ssh <destination> [ssh options].` error for a bare `ssh`; the launch command is recorded in the creator's transcript so a failed connect is still visible; `connection close ssh:<id>` matches label first, then destination when duplicates exist; the tab closes as soon as the ssh process exits and its error output dies with it, and closing the last tab quits the app; `shell ssh <host>` still opens an inline terminal card instead of a tab. The ground truth is `product/specs/ssh-tab.md` and the ssh handling in `src/`. Fix by expanding the SSH sessions section in `documentation/user-documentation/advanced-agents/harness.md` (or splitting it into a page under `documentation/user-documentation/tab-types/`) and verifying the `help.md` row still matches.

* transcript (6/10) — Transcript scrolling keys are documented in `documentation/user-documentation/getting-started/keyboard.md`, but about 6 of 12 facts are missing and no page explains the transcript's interactive behavior. Missing: `file:line` patterns in output (like `src/foo.ts:42`) are clickable and open the file in an editor tab at that line; double-clicking a previous command's prompt line re-runs it; agent tool steps appear collapsed as `N tool steps` with a click or `Ctrl+T` expanding them (keyboard.md has the key but not the behavior); the mouse wheel scrolls line by line; a scrollbar with a percentage appears in the prompt bar when scrolled up; ANSI-colored shell output renders with its colors. The ground truth is `product/specs/transcript.md`. Fix by adding a transcript section to an existing page (`documentation/user-documentation/getting-started/application.md` or `tabs.md`) or a short new page under `documentation/user-documentation/getting-started/`, covering the clickable links, re-run, and collapsed tool steps.

* harness-recording (3/10) — Harness session recording is well documented in the Recordings section of `documentation/user-documentation/advanced-agents/harness.md` (automatic recording, `.cast` files under `.janissary/recordings/`, naming, asciinema replay, cleared on fresh launch and kept on `--relaunch`, SSH excluded), with 2 of 8 facts missing, both minor. Missing: keystroke input is never recorded (output and resizes only — a privacy-relevant reassurance worth one sentence); the file is created lazily on first output, so a harness that dies instantly leaves no file. The ground truth is `product/specs/harness-recording.md`. Fix with one or two sentences in the existing Recordings section of `documentation/user-documentation/advanced-agents/harness.md`.

* sandbox (3/10) — The workspace sandbox is well documented for users in `documentation/user-documentation/advanced-agents/workspacing.md` (what works inside, write/read/secret blocking, `--offline`, the `sandboxWorkspaces` toggle, macOS-only, the not-active notice), with 1 of 10 user-facing facts missing. Missing: credential-shaped environment variables (`AWS_*`, `GITHUB_TOKEN`, `NPM_TOKEN`, `SSH_AUTH_SOCK`, and similar) are scrubbed from sandboxed processes, so a tool that needs them fails inside the workspace even though reads of the corresponding files are also blocked — worth a sentence so users understand why such tools fail. The deeper Seatbelt mechanics in the spec are engineering detail that does not belong in user docs. The ground truth is `product/specs/sandbox.md` and `src/sandbox.ts`. Fix with a sentence in the isolation list in `documentation/user-documentation/advanced-agents/workspacing.md`.

## unverified

* editor-tab — flagged by git log (live-reload of externally changed files, unsaved-changes warning, transient draft sync) against `documentation/user-documentation/tab-types/editor.md`; not yet evaluated (over this run's limit)
* embedded-web-page — flagged by git log (back/forward/reload buttons, editable address, address/label following) against `documentation/user-documentation/tab-types/web-pages.md`; not yet evaluated (over this run's limit)
* tabs — flagged by git log (metadata row with launch-agent, file-navigator, connections, and schedule buttons) against `documentation/user-documentation/getting-started/tabs.md`; not yet evaluated (over this run's limit)

## resolved

* acp — documented in `documentation/user-documentation/advanced-agents/acp-agent.md` (new page, registered in the sidebar), with the `acp` row in `help.md` extended to mention `acp reset` (removed 2026-07-19)

* quick-open — documented in `documentation/user-documentation/command-bar/quick-open.md` (new page, registered in the sidebar), with a `Cmd+P` row added to `help.md`'s Key Bindings table and a cross-link row in `documentation/user-documentation/getting-started/keyboard.md` (removed 2026-07-19)

* relaunch — the Resuming a session section of `documentation/user-documentation/getting-started/startup.md` accurately covers what `--relaunch` restores (tab order, colors, groups, transcripts, history, cwd, aliases, schedules) and what it does not (view tabs, harness tabs, workspaces), matching `product/specs/relaunch.md`; no gap (removed 2026-07-19)
