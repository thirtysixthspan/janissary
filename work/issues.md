# issues

* regression: closing the last open tab needs to trigger the quit dialog.

* move all tasks currently in /ai to /ai/tasks and update the tasks that reference each other.

* when a harness tab is focused, ctrl+a will trigger the task windiw, but it triggers the task window in the agent tab. The task window should be triggered and overlay the harness window instead.




