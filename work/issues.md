# issues

* the assistant.md ai persona should recognize work being done automatically by a harness, and instead of making suggestions that the harness will take on its own, the assistant should usffice with summarizing simply what the AI has done, is doing, is trying to do.

* closing a harness tab can block the UI. Close the tab first and terminate the harness in the background to unblock the ui.

* notifications look like: ● janus: 20:32:51 janus: this is a notification, but should look like:
● 8:32pm janus: this is a notification

* the notification window should have a keyboard focus row and mouse focus row. up and down arrows will scroll up and down the transcript.  page up and down should also scroll up and down the transcript. 

* the sidebars should have a tab strip similar to the central section. there should not be a separate tabbed UI when both the notifications and navigator tabs are present in a sidebar.
