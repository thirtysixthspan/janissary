# issues

* in the file navigator, when dragging a file, there should be a visual indicator that the drag is in process, for example, the filename following the mouse. 

* in the file navigator, when dragging a file, you should be able to drop on a directory, or any file in that directory to move the file to that directory.  




