// Central Font Awesome icon registry. Components import the semantic names below rather than
// pulling from `@fortawesome/*` directly, so each glyph's icon choice lives in exactly one place.
export {
  faFolder as openFilesIcon,
  faPlus as newTabIcon,
  faStar as unreadIcon,
  faBox as workspacedIcon,
  faBolt as autoPermitIcon,
  faCamera as screenshotIcon,
  faThumbsUp as approveIcon,
  faThumbsDown as rejectIcon,
  faBars as snapshotIcon,
  faRotateLeft as resetIcon,
  faRightLeft as dockSwapIcon,
  faCheck as selectedIcon,
  faChevronRight as promptIcon,
  faCircle as statusDotIcon,
  faCircle as dirtyMarkerIcon,
  faCaretDown as expandedIcon,
  faCaretRight as collapsedIcon,
} from '@fortawesome/free-solid-svg-icons';
